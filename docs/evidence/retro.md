# Retro — vibe-bridge

## Standing instructions (bind every run; max 10)

1. **Wire-парность:** `mcp` SDK бампается только вместе с `HERMES_VERSION`
   робота (README «Wire parity»); проверять на каждом build-ране.
2. **Fleet-канон робота — no SSH:** любой дизайн-ход, требующий SSH к Pi,
   сверять с fleet-update-policy робота и решать вслух у оператора
   (решено G1 2026-08-29: SD-бутстрап без SSH).
3. **Упакованное приложение — отдельная среда, а не деталь сборки.** Пути,
   версии, системные фреймворки и TLS ведут себя внутри `.app` иначе, чем в
   `uv run`. Всё, что их касается, проверять на собранном приложении (сегмент
   3: четыре бага, невидимых в репозитории).
4. **Проверку и необратимое действие не связывать одной командой.** Прогон
   тестов через `&&` перед `git push` — это гейт, вывод которого некому
   прочитать (сегмент 3: красный коммит в origin).

## Run stamps

| Run | Date | Commit | Outcome |
|---|---|---|---|
| v2-design | 2026-08-29 | 736cf87 | complete — REQ-01..10 закрыты |
| build (сегмент 1) | 2026-08-29 | 5246b13…58619be | T-WS/CORE/NORTH/PANEL/SOUTH/PHONE/WIZARD-a; 2 live-бага пойманы браузером |
| build (сегмент 2) | 2026-08-30 | 99403e4…2a89543 | T-ROBOT (bridge-api смержен в fleet-ветку робота a2348d9, живьём: чат+статус+события), T-PLATFORM (rename vibe-bridge, Win/Linux паки+трей, packaging-runbook); 103 теста моста + 1780 робота. Осталось: B-9 WIZARD-b, B-1/B-4 live Win/Linux+подпись (машины), телефон (владелец) |
| build (сегмент 3, B-4 macOS) | 2026-08-30 | 35e7c76…25db5de | Подписанный .app, оболочка+payload, автообновление, автозапуск; репо опубликован, канал релизов заведён. 103→186 тестов. Живьём: обновление с GitHub + переживание TCC-гранта + откат сбойной версии. Нотаризация выполнена (Accepted, staple, `spctl accepted`), DMG опубликован. Осталось: Win/Linux (B-1/B-4) |
| build (сегмент 4, дистрибуция) | 2026-08-30 | 2c6d17e…c91a89a | Слой настроек config.toml, онбординг с двумя путями подключения робота, честная дверь в панель, предупреждение о gateway без шлюза, машинно-независимый dev-путь. 186→252 теста. Живьём: миграция режима, панель в браузере, нотаризованная 0.3.0. Осталось: Win/Linux, B-18..B-20 |

| build (сегмент 5, три вопроса) | 2026-08-31 | c91a89a…HEAD | B-18/19/20 закрыты решениями: порт — контракт (спека исправлена), репозиторий робота в конфиге, согласие на READ как выбор при визийном умолчании. `config.top_up()`. 252→267 тестов, оболочка 0.4.0 нотаризована |

| build (сегмент 6, маскот) | 2026-08-31 | c91a89a…HEAD | Лицо робота: персонаж в панели и плавающий питомец (NSPanel+WKWebView), одна реализация на две поверхности. Сверка с визией задала дизайн: не сочиняет реплик, на паузе молчит, истории не ведёт. 300→302 теста, оболочка 0.5.0 нотаризована |

| build (сегмент 7, окно и питомец) | 2026-08-31 | HEAD | Окно приложения (его не было вовсе), настоящее перетаскивание вместо Electron-свойства, дыхание, меню быстрых действий. 310→325 тестов, оболочка 0.6.0 нотаризована |

## Recent log

### 2026-08-31 · build-8 · скрипт сборки падал ПОСЛЕ сборки, и я проверял старое
- **Симптом:** три раунда подряд правка не давала эффекта на экране, хотя
  тесты были зелёные, а DMG собирался.
- **Причина:** `spctl` для ненотаризованного приложения выходит с ненулевым
  кодом. Под `set -e` это убивало `build_app.sh` на предпоследней строке —
  уже после того, как DMG был готов, — и всё, что вызывающий чейнил через
  `&&` (переустановка в /Applications), молча пропускалось.
- **Стадия проявления:** 8; **владелец:** 7 (скрипт сборки).
- **Фикс (grade: code):** `|| true` на ожидаемо-ненулевой команде.
- **Чек на будущее:** если правка «не действует», сначала проверить КОД
  ВЫХОДА пайплайна доставки, а не искать баг в правке. Зелёные тесты ничего
  не говорят о том, доехал ли артефакт.

### 2026-08-31 · build-8 · borderless окно не принимает клавиатуру
- **Симптом:** в поле ввода виджета нельзя было напечатать ни символа.
- **Причина:** `NSWindowStyleMaskBorderless` заставляет
  `canBecomeKeyWindow` возвращать NO — клавиатура до окна не доходит в
  принципе. Выбор borderless был сделан ради вида, а не ради поведения.
- **Фикс (grade: code):** titled + `FullSizeContentView` со скрытым
  заголовком (выглядит так же) и `becomesKeyOnlyIfNeeded`, чтобы фокус
  забирался только контролом. Заодно снят `movableByWindowBackground`: он
  съедал mouse-down и клик не доходил до страницы.
- **Чек на будущее:** выбирая стиль окна, проверять не только вид, но и
  `canBecomeKeyWindow` — иначе поверхность выглядит правильно и не работает.

### 2026-08-31 · build-7 · CSS из другого рантайма, принятая за рабочую
- **Симптом:** питомца нельзя было сдвинуть с места.
- **Причина:** `-webkit-app-region: drag` — расширение **Electron**. В
  `WKWebView` его нет, свойство просто игнорируется, и никакой ошибки при
  этом не возникает: CSS молча пропускает то, чего не знает.
- **Фикс (grade: code):** страница шлёт дельту через WKScriptMessageHandler,
  окно двигает себя; тест проверяет, что свойства нет в правиле.
- **Чек на будущее:** CSS-свойство с префиксом рантайма (`-webkit-app-region`,
  `-moz-*`) проверять на целевом движке, а не считать поддержанным по виду.
  Молчаливое игнорирование — худший вид отказа: он не оставляет следов.

### 2026-08-31 · build-7 · два своих же контракта разъехались на одном слове
- **Симптом:** маскот показывал «думает» после каждого сообщения роботу и
  затем молчал. Выглядело как неответ робота.
- **Причина:** `RobotClient.chat` возвращает `{ok, reply}`, а новый код читал
  `answer["text"]`. Чат работал всё это время; молчал только маскот.
- **Фикс (grade: code):** читаем `reply`; тест держит контракт с обеих сторон
  сразу — вызывает клиент и проверяет, что в web.py читается тот же ключ.
- **Чек на будущее:** подключая новый потребитель к существующей функции,
  прочитать её return, а не угадать имя поля по смыслу.

### 2026-08-31 · build-7 · медленный набор — это ожидание, а не работа
- **Симптом:** прогон вырос с 10 секунд до четырёх минут.
- **Причина:** 24% CPU выдали ответ сразу — это I/O. `tailscale_ips` и
  `tailnet_dns_name` шеллятся в CLI и вызываются на каждой сборке приложения;
  добавленные веб-тесты умножили это на шестьдесят.
- **Фикс (grade: code):** кэш на минуту (верно и для продакшена). Попутно сам
  же и сломал: очистка кэша в conftest перед каждым тестом свела его на нет —
  119 секунд вместо 12.
- **Чек на будущее:** при жалобе на скорость сначала смотреть процент CPU;
  низкий процент означает, что искать надо subprocess и сеть, а не алгоритм.

### 2026-08-31 · build-6 · слепая замена строки размножила код
- **Симптом:** панель определяла тикер маскота трижды, и одна копия попала
  внутрь `attachRobot` — каждая ручная привязка робота заводила ещё один
  `setInterval`.
- **Стадия проявления:** 6 (браузерная проверка); **владелец:** 5.
- **Причина:** правка делалась `str.replace` по короткому якорю
  `loadOnboarding();`, который встречался в трёх местах. Замена без счёта
  вхождений — это ставка на то, что якорь уникален, а он не был.
- **Фикс (grade: code):** дубликаты сняты, тест считает копии тикера и
  таймера.
- **Чек на будущее:** правя разметку или скрипт заменой по строке, сначала
  посчитать вхождения якоря; при >1 — править по диапазону строк.

### 2026-08-31 · build-5 · шаблон конфига — это инструкция, и она отстаёт
- **Симптом:** после установки 0.4.0 новые настройки (`ask_for_read`,
  `[robot] repo`) не появились в `config.toml` этой машины: файл был создан
  0.3.0, а шаблон пишется только при создании.
- **Стадия проявления:** 8 (live-проверка); **владелец:** 5 — слой настроек
  спроектирован без ответа на вопрос «что будет со второй версией».
- **Причина:** файл задуман документацией (комментарии объясняют каждое
  значение), но документация обновлялась только у новых установок. Владелец
  мог узнать о переключателе лишь из release notes.
- **Фикс (grade: code):** `top_up()` дописывает недостающие ключи с их
  комментариями, значений не трогает, битый файл не переписывает.
- **Чек на будущее:** любой формат, который мы называем документацией для
  человека, должен иметь путь обновления у тех, у кого он уже есть.

### 2026-08-30 · build-4 · написано, протестировано, не вызвано — второй раз
- **Симптом:** после установки 0.3.0 мост ушёл с loopback на tailnet-адрес и
  перестал быть доступен своей же цепи через agentgateway.
- **Причина:** `migrate_from_state` — перенос режима для уже работающих
  установок — была написана и покрыта юнит-тестом, но **никем не вызывалась**.
  Юнит-тест функции зелёный ничего не говорит о том, что её кто-то зовёт. Тот
  же класс, что `layout.prune` в сегменте 3.
- **Фикс (grade: code):** `prepare_settings()` вызывается на старте, и тест
  теперь про ВЫЗОВ, а не про функцию.
- **Чек на будущее:** для каждой новой функции, которая должна что-то делать
  при старте, писать тест точки входа, а не только самой функции. Дважды
  подряд — значит правило, а не случайность.

### 2026-08-30 · анализ · продукт проверялся только на машине автора
- **Симптом:** отчёт «приложение работает» был верен и неполон: путь нового
  пользователя не проверялся ни разу. Разбор нашёл, что дефолтный режим
  противоречит собственной спеке, конфига нет вообще, у человека с готовым
  роботом нет пути, панель показывает зашитый текст режима, а `.plist` и
  `run.sh` содержат пути автора.
- **Причина:** все живые проверки шли на этой машине, где всё уже настроено.
  «Работает» без указания «у кого» — половина утверждения.
- **Фикс (grade: process):** проверять путь установки с нуля, а не только
  обновление существующей.
- **Чек на будущее:** в отчёте о работоспособности называть, ДЛЯ КОГО она
  проверена; «работает на машине автора» и «работает у нового пользователя» —
  разные утверждения, и второе дороже.

### 2026-08-30 · build-3 · один долгоживущий каркас прятал пять дефектов сборки
- **Симптом:** первая же пересборка `.app` с нуля не прошла: версия в
  `Info.plist` осталась старой, зачистка не сработала, `briefcase create` упал
  на зависимостях, подпись сломалась о `.dSYM`, а затем о нерасставленный
  stub-бинарь.
- **Стадия проявления:** 7 (сборка); **владелец:** 7 — скрипт с самого начала
  ни разу не проверялся на чистом каркасе.
- **Причина:** сборка была написана и отлажена поверх каркаса, созданного
  один раз руками. Всё, что делает `briefcase create`/`build`, оказалось вне
  скрипта, и его отсутствие было невидимо, пока каталог переживал прогоны.
- **Фикс (grade: code):** каркас пересоздаётся при смене версии (вместе со
  всем каталогом платформы), `briefcase build` добавлен, ошибка установки
  зависимостей терпится намеренно и каркас проверяется по факту, `.dSYM` и
  тестовые пакеты вычищаются, версия в `Info.plist` — гейт со стопом.
- **Чек на будущее:** скрипт сборки прогонять от пустого `build/` хотя бы раз
  за ран — иначе он проверяет не себя, а сохранившийся артефакт.

### 2026-08-30 · build-3 · «обновляется сам» было написано, но не сделано
- **Симптом:** README и SCN-021 обещали фоновое обновление; в коде была только
  кнопка в панели. Обнаружено при попытке довести приложение до рабочего вида.
- **Владелец:** стадия 5 предыдущего сегмента — сценарий был написан целиком,
  реализована половина, и разрыв никто не сверил.
- **Фикс (grade: code):** `AutoUpdater` с политикой журнала; заодно подключён
  `prune`, который был написан и никем не вызывался.
- **Чек на будущее:** при закрытии сценария сверять КАЖДЫЙ его шаг с кодом —
  «Система проверяет в фоне» это отдельный шаг, а не описание кнопки.

### 2026-08-30 · build-3 · чтение состояния версий уронило мост
- **Симптом:** через минуту после успешного обновления мост перестал
  стартовать: работавшая версия 0.1.2 оказалась в карантине.
- **Стадия проявления:** 6 (live-проверка); **владелец:** 5 (build — дизайн
  раскладки).
- **Причина:** `resolve()` совмещал две роли — выбрать версию и осудить ту,
  чей launch-маркер пережил старт. На нём же стоял `active_version()`, который
  зовёт `/api/version`. Любой запрос панели внутри 20-секундного окна
  приживания помечал ЖИВУЮ версию сбойной. Классическая мутация в чтении.
- **Фикс (grade: code):** `usable()`/`active_version()` — чистые; пишет только
  `resolve_for_launch()`, и зовёт его один бутстрап. Тест
  `test_reading_the_active_version_never_quarantines_anything`.
- **Чек на будущее:** функция, которую зовёт HTTP-ручка, не имеет права писать
  на диск; при добавлении ручки — проверить весь путь до диска.

### 2026-08-30 · build-3 · «обновлений нет» вместо «не смог спросить»
- **Симптом:** собранное приложение сообщало «обновлений нет», хотя релиз был
  опубликован. Канал при этом был недоступен.
- **Причина, двойная:** (1) `check()` возвращал `None` и на «нечего ставить», и
  на любую ошибку — панель показывала спокойствие вместо диагноза; (2) у Python
  внутри `.app` нет системного хранилища CA, и каждый запрос падал с
  `CERTIFICATE_VERIFY_FAILED`. Первое пряталo второе.
- **Фикс (grade: code):** тип `Check` с раздельными `up_to_date` и `error`,
  ошибка уходит в журнал; апдейтер берёт корни из `certifi` и никогда не
  понижается до непроверяемого контекста.
- **Чек на будущее:** «ничего не нашли» и «не смогли посмотреть» — всегда
  разные ответы; в упакованном приложении отдельно проверять сеть, а не только
  запуск.

### 2026-08-30 · build-3 · запушен красный коммит
- **Симптом:** коммит `f6a...` ушёл в origin с 37 падающими тестами.
- **Причина:** прогон тестов и `git push` стояли в ОДНОЙ команде через `&&`;
  вывод тестов я прочитал уже после того, как пуш состоялся. Гейт был
  формально выполнен и фактически не сработал.
- **Фикс (grade: process):** прогон и пуш — разные вызовы; результат читается
  до пуша. Исправлено следующим коммитом (`25db5de`).
- **Чек на будущее:** никогда не связывать проверку и необратимое действие
  одной командой — проверка, вывод которой некому прочитать, не проверка.

### 2026-08-30 · build-3 · четыре бага, видимых только в бандле
- **Симптом:** якорь доверия искался от `vibebridge` (после первого обновления
  тот уходит из бандла — обновление сработало бы ровно один раз); версия
  оболочки бралась из payload (несовместимый payload прошёл бы проверку);
  уведомление экранировалось `repr()` и AppleScript его не принимал; карта
  способностей объявляла `screenshot: available`, потому что `Quartz` не попал
  в бандл и проба не могла ответить.
- **Общая причина:** всё это невидимо в dev-окружении, где пакеты лежат рядом,
  а системные фреймворки доступны. Разница «репозиторий против бандла» — это
  отдельная среда, а не деталь упаковки.
- **Фикс (grade: code):** якорь на `vbboot`; версия оболочки из `Info.plist`;
  AppleScript-экранирование; `pyobjc-framework-Quartz` в бандл, и «не смог
  ответить» больше не превращается в «доступно».
- **Чек на будущее:** каждый шов, который ведёт себя иначе внутри `.app`
  (пути, версии, системные фреймворки, TLS), проверять НА СОБРАННОМ
  приложении, а не в `uv run`.

### 2026-08-30 · build-2 · пейринг молча отключил MCP-путь робота (~15 ч)
- **Симптом:** после пейринга робот получал 401 на свои mac_*-инструменты
  через гейтвей; никто не заметил — north-путь после пейринга не перегоняли.
  Вскрыто проверкой цепи после rename-волны.
- **Причина:** BearerGuard ключевался на «robot_token существует», а пейринг
  создаёт токен, не меняя режима. Наличие секрета ≠ standalone (ADR-0002).
  Тот же класс, что R1.49 робота: смена в одном месте тихо ломает соседнее.
- **Фикс (grade: code):** гейт ключуется на state.mode; регрессионный тест
  `test_mcp_gateway_mode_ignores_paired_token`.
- **Чек на будущее:** любой шов auth/state → прогон ОБОИХ путей (loopback +
  гейтвей) в той же проверке; тест в наборе.

### 2026-08-29 · build-1 · ACT-консент замораживал event loop (60 с темноты)
- **Симптом:** во время ожидания согласия панель, SSE и /api/state не
  отвечали; пропущенный запрос выглядел как «pending: 0». Поймано только
  браузерной live-проверкой T-PANEL — юнит-тесты звали dispatch из своего
  треда и не видели блокировки.
- **Стадия проявления:** 6 (browser-check); **владелец:** 5 (build T-WS —
  сгенерированные async-обёртки звали блокирующий dispatch inline).
- **Причина:** consent.request блокирует до 60 с; await в event loop = стоп
  всего процесса asyncio.
- **Фикс (grade: code):** dispatch → `anyio.to_thread.run_sync` (server.py);
  регрессионный тест `test_act_tool_does_not_block_event_loop` (ticker
  дышит, пока ACT ждёт).
- **Чек на будущее:** тест в наборе; правило — любой блокирующий вызов из
  async-обёртки только через to_thread.

### 2026-08-29 · build-1 · 307-редирект маунта уводил робота в сам гейтвей
- **Симптом:** Mount("/mcp") давал 307 → Location из Host; для гейтвея
  (Host=100.x:4000) редирект указывал на него самого.
- **Владелец:** T-WS (выбор точки маунта); всплыло на live-проверке T-NORTH.
- **Фикс (grade: code):** транспорт на своём `/mcp`, inner-app в корень
  последним; контракт «/mcp без слэша, без редиректа» записан в web.py.
- **Чек:** live-семантика 421/406 проверяется на каждом рестарте руками
  T-NORTH-проверки; юнит `test_mcp_rejects_foreign_host…`.

### 2026-08-29 · v2-design · vision написан до чтения контракта формата
- **Симптом:** первый вариант `docs/ux/vision.md` — с русскими заголовками
  секций; контракт ux-contract v4 ключует линтер на английские заголовки
  (U030). Обнаружено при чтении `scenario-format.md` ПОСЛЕ написания файла,
  исправлено в том же ране.
- **Стадия проявления:** 3 (spec/UX-трек); **владелец:** стадия 1 (docs
  study — формат-контракт тоже контракт, его читают до письма).
- **Причина:** SKILL.md скила ссылается на контракт отдельным файлом;
  порядок «скил → сразу писать» пропустил referenced-контракт.
- **Фикс (grade: process):** для цепочки docs/ux читать
  `references/scenario-format.md` до первого файла.
- **Чек на будущее:** `docs/ux/lint.py` теперь в репо и ловит U030
  механически; при build-ране — включить его в pre-commit/CI.

### 2026-08-31 · build-14 · четыре круга на «голова прыгает», и почему их было четыре
- **Симптом:** владелец четыре раза подряд сообщал одно и то же — голова
  робота меняет место при клике. Каждый круг я правил АРИФМЕТИКУ якоря:
  сначала якорь на рамке окна, потом на углу персонажа, потом мемоизация
  перерисовки. Пятый раз пришёл с заглавными буквами: «ОН НЕ ДОЛЖЕН МЕНЯТЬ
  МЕСТО ВООБЩЕ ПРИ КЛИКАХ».
- **Стадия проявления:** 6 (проверка на экране); **владелец:** стадия 2
  (декомпозиция — одно окно на две роли было архитектурным выбором, а не
  деталью реализации).
- **Причина:** ресайз окна — это релэйаут, а релэйаут — это шанс, что
  содержимое встанет иначе. Никакая арифметика якоря этот шанс не убирает.
  Четыре круга ушли на уточнение вычисления вместо того, чтобы убрать
  вычисление: окно головы теперь константного размера, и ни одна строка его
  не ресайзит.
- **Фикс (grade: design):** виджет — два окна. Голова в своём, неизменном;
  всё, что она говорит, в компаньоне, который ресайзится сколько хочет.
- **Чек:** `test_a_resize_never_touches_the_characters_window` — если этот
  тест может упасть, голова может прыгнуть. Плюс `side_frame()` вынесена
  чистой функцией: геометрия, которую можно проверить только глазами,
  проверяется один раз.
- **Правило:** симптом, повторённый третий раз, — это не недоделанный фикс, а
  неверный уровень. Третий заход в один и тот же файл по одной причине —
  сигнал сменить слой, а не коэффициент.

### 2026-08-31 · build-14 · `except: pass` спрятал ту самую ошибку, ради которой стоял
- **Симптом:** после разделения на два окна клик по голове перестал открывать
  что-либо. Ни строки в журнале, ни исключения — клик выглядел как клик,
  которого не было. Ушло три сборки на диагностику вслепую.
- **Причина:** `_Bridge.handle` был обёрнут в `except Exception: pass` «чтобы
  питомец никогда не уронил мост», а страница-компаньон получала
  `window.vbToggle && window.vbToggle()` — то есть тихо ничего не делала,
  когда `vbToggle` не существовало. Настоящая же ошибка была в редиректе за
  токеном: `RedirectResponse("/mascot")` пересобирал адрес заново и срезал
  `surface`, так что ОБА окна поднимались как `pet`.
- **Стадия проявления:** 6; **владелец:** стадия 5 (сборка — глотание
  ошибки написано моей рукой в этом же ране, вместе с фейл-опен-проверкой в
  JS).
- **Фикс (grade: code):** редирект переносит все остальные параметры;
  `handle` докладывает провал в журнал; JS брошен вместо no-op; каждое окно
  пишет одну строку «окно «side» открылось» при подъёме — именно она назвала
  бы причину в первом круге вместо четвёртого.
- **Чек:** `test_the_token_redirect_keeps_the_surface_marker`,
  `test_each_window_announces_itself_once`.
- **Правило (третий случай этого класса за проект):** «никогда не роняй
  мост» — это про ПРОДОЛЖИТЬ РАБОТУ, а не про МОЛЧАТЬ. Обработчик, который
  ничего не пишет, превращает баг в отсутствие события, а отсутствие события
  нельзя отладить.

### 2026-08-31 · build-14 · приписал владельцу баг, а это был владелец
- **Симптом:** измерения показали, что компаньон открывается и через ~4 с
  закрывается сам. Я построил четыре гипотезы о гонках в поллерах и собрал
  инструментированную сборку.
- **Причина:** владелец нажимал «Закрыть» и таскал голову, пока я мерил.
  Инструментация назвала виновника первой же строкой: `кнопка Закрыть`.
- **Фикс (grade: process):** на живой машине владельца наблюдение — не
  эксперимент. Прежде чем объявить поведение багом, надо отличить свои
  события от чужих: помечать источник, а не выводить его.
- **Чек:** канал сообщений виджета теперь пишет в журнал провалы и подъём
  окон, так что источник события различим без специальной сборки.

### 2026-08-31 · build-15 · загадочное уведомление было нашим тестом, и я дважды сказал, что нет
- **Симптом:** владелец трижды присылал скриншот уведомления «заголовок / текст»
  с иконкой Script Editor. Я дважды ответил, что мост его не посылает: в
  журнале таких записей нет, и `display notification` с такими словами не
  пишет ни мост, ни робот. Оба раза — правда, и оба раза вывод неверный.
- **Причина:** `tests/test_platforms.py::test_notifier_never_raises` прятал
  `desktop_notifier`, чтобы дойти до osascript-ветки, и вызывал нотифаер
  ПО-НАСТОЯЩЕМУ. Каждый прогон набора ставил владельцу настоящий тост от
  имени Script Editor — а сборка гоняет набор первым шагом, так что мои
  собственные сборки и производили те скриншоты, на которые я смотрел.
- **Стадия проявления:** 6; **владелец:** стадия 5 (тест, выходящий за
  процесс, — дефект теста, а не системы).
- **Почему я его не нашёл:** искал по КОДУ ПРОДУКТА и по журналу моста.
  Тесты в область поиска не входили — «журнал пуст, значит не мы» верно ровно
  до того момента, когда стреляет то, что журнала не ведёт.
- **Фикс (grade: code + process):** тест подменяет `subprocess.run` и
  проверяет обе ветки; в `conftest.py` автопредохранитель на весь набор.
- **Чек:** предохранитель проверен подсаженным дефектом. Первая версия —
  бросок в момент вызова — дефект ПРОПУСТИЛА: `_osa` ловит `Exception` и
  честно возвращает «уведомление не показано», так что подсадка прошла
  зелёной. Работает только вердикт на teardown, который код под тестом
  перехватить не может.
- **Правило:** «этого нет в наших логах» — не доказательство «это не мы».
  Область поиска источника внешнего эффекта включает тесты, сборку и
  инструментарий, а не только продукт.

### 2026-08-31 · build-15 · пчела на уведомлении была нашей иконкой, а не библиотечной
- **Симптом:** тост приходил с иконкой пчелы. Я измерил, что macOS показывает
  значок ПУБЛИКУЮЩЕГО бандла, и заключил, что параметр `icon` бесполезен, а
  пчела принадлежит `desktop-notifier`. Записал это как измеренный факт.
- **Причина:** измерение было верным, вывод — нет. Пчела — логотип BeeWare,
  дефолтная иконка Briefcase, и она была НАШЕЙ: `briefcase create` ставит
  ресурсы приложения ПОСЛЕ установки зависимостей, а этот шаг у нас всегда
  падает и глушится `|| true`, поэтому до копирования иконки дело не доходило
  ни разу за 14 релизов. Пчела стояла на приложении в /Applications — и,
  следовательно, на каждом уведомлении.
- **Стадия проявления:** 6; **владелец:** стадия 7 (упаковка — шаг, чей
  результат никто не сверял с ожиданием).
- **Фикс (grade: code):** `briefcase update --update-resources` и
  побайтовое сравнение иконки в бандле с `packaging/vibe-bridge.icns` как
  гейт сборки.
- **Правило:** «конфиг называет ресурс» — не доказательство, что бандл его
  несёт. Шаг упаковки, чей выход не сверяется, — это шаг, который может не
  выполняться годами, ничего не ломая заметно.
