# Verification ledger — vibe-bridge

Каждый выкаченный REQ получает строку: как проверен, кем, когда.
Seeded 2026-08-29 (v2 design run). Код этим раном не менялся; строки
появятся с первым build-раном.

| REQ | Shipped | Verified | How | When |
|---|---|---|---|---|
| T-WS (walking skeleton) | web.py/state.py/webui + app.py провязка | yes | 35 тестов зелёные (`pytest -q`); live: `launchctl kickstart` → MCP-клиент listed 10 tools на `127.0.0.1:48620/mcp`, gateway-пинги в `/tmp/mac-bridge.err`, панель 401→303→200 + `/api/state` | 2026-08-29 |
| T-CORE (ST-003, ST-010 ядро; SCN-002/003/005/011/018) | consent v2 (id, first-wins, resolve_by_id), probe-карта способностей + честный отказ до консента, журнал line+ротация, API pause/revoke/capabilities | yes | 49 тестов зелёных; ruff clean; ux-lint 0 err; live: `/api/capabilities` отдаёт карту (screenshot=available → TCC уже выдан этому питону), pause через API включает/выключает | 2026-08-29 |
| T-NORTH (ST-001 транспорт; ADR-0002) | нейтральные имена + 10 `mac_*`-алиасов; DNS-rebinding-защита ON с allowlist (loopback+tailnet `:*`); фикс 307-редиректа маунта (транспорт на своём /mcp, mount в корень); standalone-бинд | yes | 54 теста; live: чужой Host→421, гейтвейный Host→транспорт, e2e через agentgateway ролью robot: 20 tools, `mac_notify` исполнен (`ok:true`, уведомление показано) | 2026-08-29 |
| T-PANEL (ST-001 UI, ST-010; SCN-001/002/011 live) | панель со вкладками (дашборд/журнал/настройки, чат — честная заглушка), журнал с диска (фильтры+пагинация, `/api/journal`), карта способностей в настройках, трей-состояния, «решено с другой поверхности»; **фикс: dispatch → worker-thread (ACT замораживал event loop на время консента)** | yes | 59 тестов (+ регрессия loop-freeze); браузерная проверка Chrome: карточка согласия live по SSE → клик «Разрешить» → робот получил `opened Calculator`, Calculator запустился; SCN-002 живьём (таймаут → отказ → строка в ленте) | 2026-08-29 |
| T-SOUTH (ST-005/009 мост-сторона; SCN-007/008/009/012 mock) | RobotClient (чат Hermes 150с+retry-once; status offline-since sticky; update; events SSE), `/api/robot/*`, чат-вкладка и живая карточка робота, event-consumer с пауза-сводкой; поля робота в state | **live-verified** | 88 тестов; ЖИВЬЁМ 2026-08-29: пейринг одноразовым токеном через tailnet-HTTPS → робот «Вася» в панели (v1.1.0+178, hermes, online), чат через мост — ответ мозга «Мост с компьютера функционирует», событие telegram_message дошло до ленты панели по SSE; пойман и закрыт live-401 (bearer теперь на всех вызовах юга — тест bearer-everywhere) | 2026-08-29 |
| T-WIZARD-a (ST-007/008 ядро; SCN-013 файловая механика, SCN-015 мост-сторона) | wizard.py: генераторы firstrun.sh (официальная Imager-механика: systemd.run + самоудаление), NM-профиль, provision-юнит/скрипт (shred токена, curl /pair с ретраями, 0600 креды), cmdline_patch идемпотентный; `/pair` одноразовый (compare_digest, burn, аудит обеих веток, перенастройка юга на лету, уведомление); `/api/wizard/{pairing/start,disks,prepare}`; поиск boot-раздела; UI-карточка в настройках | unit+live-API | 86 тестов (8 новых, все зелёные с первого прогона); live: `/api/wizard/disks` честно `[]` без карты; wire-контракт пейринга записан в спеку §6 для M-ROBOT | 2026-08-29 |
| T-PHONE (ST-002; SCN-004 пуш-половина, SCN-019) | push.py (VAPID-генерация в state 0600, подписки с заменой по endpoint, прунинг 401/403/404/410, send never-raises), consent-push watcher, `/api/push/*` + `/api/phone`, PWA-шелл (manifest standalone, sw.js: offline-fallback + Android-кнопки Allow/Deny + iOS-тап, offline.html, PNG-иконки stdlib'ом), настройки «Телефон» с точной командой serve | unit+live-shell | 78 тестов (7 новых); live: sw/manifest/icon отдаются 200, `/api/phone` детектит MagicDNS владельца (`<host>.<tailnet>.ts.net`; реальное имя не пишем — репо публичное) и serve_active=false с командой; **`tailscale serve` требует включения Serve в админке тейлнета — Human step** (ссылка в отчёте) | 2026-08-29 |
| T-PLATFORM (ST-011; SCN-018 Win/Linux ветки; ADR-0003/0005) | rename macbridge→vibebridge (репо+машина+плист, миграция state/логов с сохранением пейринга); Win/Linux capability-паки (единый контракт, честная деградация по таблице); трей-абстракция (rumps/pystray); extras [macos]/[windows]/[linux]; packaging-runbook | unit + macOS-live | 103 теста (14 новых: паки+трей); macOS живьём после rename+refactor: робот online, 10 способностей, gateway-путь исполняет mac_notify; **Win/Linux live + подпись/нотарификация — B-1/B-4 (нужны машины и сертификаты)**; пойман и закрыт пост-rename баг: пейринг молча включал bearer в gateway-режиме (BearerGuard ключевался на наличии токена) | 2026-08-30 |
| B-4 macOS (ST-011; SCN-021/022; ADR-0006) | подписанный `.app` (Developer ID, hardened runtime, LSUIElement, TCC-usage-строки), оболочка+payload с Ed25519-проверкой до распаковки, откат в том же запуске, автозапуск через SMAppService, карточка «Приложение» в панели, `scripts/build_app.sh` + `build_payload.py` + `release_key.py`, публичный репозиторий и канал релизов | **live-verified (кроме нотаризации)** | 186 тестов (83 новых); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-30 из `/Applications`: `codesign --verify --deep --strict` → valid + satisfies DR, `flags=0x10000(runtime)`, TeamIdentifier KJ35UYYL22, оба ослабляющих entitlement = false; `spctl` → `rejected/Unnotarized Developer ID` (документированный до-нотаризации вердикт); MCP отдал 20 инструментов, `screenshot` исполнился реально; автозапуск `SMAppService` → `enabled`; второй экземпляр отказал по занятому порту; обновление 0.1.1→0.1.2 скачано с GitHub Releases, подпись сошлась, после перезапуска `source: payload`; **TCC-грант пережил обновление — скриншот работает без повторной выдачи**; подсаженная сбойная версия отвергнута, карантин, мост поднялся на предыдущем коде В ТОМ ЖЕ запуске. Пойманы и закрыты 6 живых багов (см. retro). **Нотаризация выполнена 2026-08-30**: submission a102c001-0f92-4846-b65a-f2bb86ff0859 → `status: Accepted`, staple лёг на `.app` и `.dmg` (`stapler validate` → worked), `spctl -a -vvv -t exec` → `accepted / source=Notarized Developer ID`; нотаризованная сборка переустановлена в /Applications, MCP отдаёт 20 инструментов, скриншот работает — права уцелели; DMG опубликован как релиз `shell-v0.1.0` | 2026-08-30 |
| B-4 macOS 0.2.x (SCN-021 фоновая половина; B-13 иконка) | AutoUpdater (проверка раз в 6 ч, политика журнала «успех молчит, повтор сбоя не дублируется», prune после установки, выключатель у владельца), канал на Atom-фиде с разделением тегов payload/оболочка, иконка кодом из токенов пака + плитки PWA, сборка починена в пяти местах | **live-verified** | 204 теста (+18); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-30: оболочка 0.2.1 подписана и нотаризована (`spctl` → accepted), `Info.plist` 0.2.1 совпадает с DMG (гейт в скрипте); мост нашёл payload v0.2.2 на новом канале, поставил, после перезапуска `running 0.2.2 | source payload`, **скриншот работает — права уцелели**; робот «Вася» online через приложение; PWA-плитка отдаёт настоящий знак. Пойманы живьём: релиз оболочки затенил payload-канал через `latest`; REST API упёрся в лимит 60/час на IP; `Info.plist` не обновлялся при бампе; отсутствовал шаг `briefcase build`; `.dSYM` роняли подпись | 2026-08-30 |
| Дистрибуция 0.3.0 (SCN-017 в правду, SCN-023/024) | config.toml с приоритетом env→файл→умолчание и честной деградацией; дефолт режима standalone + миграция уже работающих установок; /api/settings отдаёт «в силе» отдельно от «в файле»; gateway_reachable и предупреждение об отсутствующей границе; /api/robot/attach и /api/onboarding; HTML-дверь вместо 401-JSON; run.sh и dev-плист машинно-независимы; README переписан | **live-verified** | 252 теста (+66); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-30: оболочка 0.3.0 нотаризована (`spctl` → accepted); config.toml создан 0600, миграция сохранила `gateway` и вернула бинд на loopback; панель проверена в браузере (chrome-devtools) — приветствие, форма ручной привязки, карточка «Доступ и настройки» с настоящим режимом; `/` без ключа отдаёт HTML; робот «Вася» online, MCP 20 инструментов, скриншот работает. Пойманы живьём: миграция была написана и НЕ вызывалась (мост ушёл с loopback на tailnet-адрес); кнопка «Скопировать ссылку» показывала исходник своего обработчика | 2026-08-30 |
| Дистрибуция 0.4.0 (B-18/19/20; SCN-023 расширен) | `consent.ask_for_read` (умолчание визийное — READ в журнале сразу после), `robot.repo` в конфиге и в визарде, отказ по занятому порту называет держателя (lsof) и путь смены, `config.top_up()` дописывает новые ключи в существующий файл | **live-verified** | 267 тестов (+15); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-31: оболочка 0.4.0 нотаризована (`spctl` → accepted); top_up дописал `ask_for_read` и `[robot]` в конфиг, созданный 0.3.0, не тронув `mode=gateway` и `port`; строгий режим включён из панели → перезапуск → **скриншот через MCP запросил согласие, панель показала карточку, после «Разрешить» исполнился**; умолчание возвращено. Сверка с визией §9.1/§5 записана в коммит | 2026-08-31 |
| Маскот 0.5.0 (SCN-026; vision §5.3/§6/§9) | `mascot.py` — что он вправе говорить (только слова робота и действия моста; на паузе тишина, реплика истекает, истории нет); `mascot.js` — персонаж из токенов пака, состояние несёт форма а не только цвет; `mascot.html` — страница плавающего окна; `mascot_window.py` (сейчас `vibebridge/desktop.py`) — NSPanel + WKWebView, non-activating, честная деградация вне macOS; `/api/mascot`, `/mascot`; переключатель в настройках и в меню-баре | **live-verified** | 302 теста (+33); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-31: оболочка 0.5.0 нотаризована (`spctl` → accepted); питомец показан поверх экрана с прозрачным фоном; ACT-запрос через MCP → питомец пожелтел, глаза расширились, сказал словами робота «прочитать буфер обмена Мака» и дал три кнопки; **отклонено с самого питомца — исполнения не было**; на паузе посерел, глаза закрылись, ни реплики ни кнопок; персонаж в панели проверен в браузере. Пойманы: три копии тикера с утечкой таймера в `attachRobot` (моя же правка), обрезанная кнопка «Отклонить» при ширине окна 300, слипшаяся подпись версии робота | 2026-08-31 |
| Окно и живой питомец 0.6.0 (SCN-026 расширен, SCN-027) | `desktop.py`: MainWindow (NSWindow+WKWebView с панелью) и MascotWindow с мостом сообщений — перетаскивание, подгонка размера, запрос окна; дыхание в покое с гашением по prefers-reduced-motion; меню быстрых действий с настраиваемыми фразами (`mascot.actions`); кэш tailnet-запросов | **live-verified** | 325 тестов (+22); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-31: оболочка 0.6.0 нотаризована (`spctl` → accepted); у процесса стало 2 окна вместо 0, окно с панелью на экране; синтетическим Quartz-событием питомец перетащен — позиция сместилась ровно на заданную дельту (−96,−96); клик открыл меню со всеми тремя фразами, окно выросло 120×96 → 230×322 и вернулось; фраза «Как дела?» дошла до мозга (маскот перешёл в thinking); робот ответил «Жив» и маскот это произнёс. Пойманы: `-webkit-app-region` (Electron-only, WKWebView игнорирует — питомец не двигался вовсе); маскот читал `text` вместо `reply` и молчал после каждого чата; набор замедлился до 4 минут на subprocess-вызовах tailscale | 2026-08-31 |
| Система робота в панели (B-26; SCN-026 статус) | Робот: `/bridge/system` — проекция канонического `dashboard_data.collect()`, запрос не роняется при сломанном датчике, тест гоняет проекцию против НАСТОЯЩЕГО сборщика (пути `cpu_temp`/`uptime`/`throttled` лежат внутри `system`, мок такого не ловит). Мост: `RobotClient.system()`, `/api/robot/system`, карточка «Система робота» — температура, нагрузка, память, диск, троттлинг, воздух, сервисы точкой И словом | **частично live** | Робот: 23 теста, `ci_local.sh` зелёный целиком, коммит ebaf994 запушен в `feat/agent-first-os` — **заимствованное свидетельство: этот коммит в репозитории робота, из моста не проверяется**. Мост: 357 тестов (+7), ruff clean, ux-lint 0 err, оболочка 0.9.0 нотаризована. ЖИВЬЁМ 2026-08-31: `/api/robot/system` отдаёт `ok:false` с «робот ещё не обновился» — это и есть проверка честной деградации; обновление робота запускалось дважды через `/api/robot/update`, он дошёл до build 132 (коммит на два позади) и подхватит остальное своим таймером (ADR-0001, без SSH). **Сверка с настоящими цифрами Pi — B-30** | 2026-08-31 |
| Виджет двумя окнами 0.14.0 (SCN-026/027; «ОН НЕ ДОЛЖЕН МЕНЯТЬ МЕСТО ВООБЩЕ ПРИ КЛИКАХ») | Голова и то, что она говорит, разведены по ДВУМ окнам: `PET_SIZE` — константа, окно головы не ресайзится ничем, единственный писатель его origin — обработчик драга; окно-компаньон живёт по чистой `side_frame()` (нижний-правый угол на верхнем-правом углу головы, клампинг по экрану) и скрывается альфой, а не `orderOut` (у выведенного за экран WKWebView душат таймеры, а именно он должен замечать приходящее уведомление); `acceptsFirstMouse` — своя ветка `WKWebView`; провалы канала уходят в журнал вместо `except: pass`, каждое окно докладывает, что поднялось | **live-verified** | 398 тестов (+11); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-31, измерено через `CGWindowListCopyWindowInfo`, не на глаз: голова `(1596, 871, 104, 104)` **побитово одинакова на пяти замерах** — старт, открытие, +1, +2, +3 с; компаньон открывается ПЕРВЫМ кликом (140 → 275 по высоте), правые края сходятся в 1700, низ 877 = верх головы 871 + перекрытие 6; второй клик закрывает. Пойманы живьём: редирект за токеном срезал `surface`, и ОБА окна поднимались как `pet` — компаньона не существовало, клик молча ничего не делал (журнал: два `hello`, оба `surface = pet`); `WKWebView` отвечает NO на `acceptsFirstMouse`, поэтому первый клик по неактивному окну съедался, открывал только второй; «сама закрывается через 4 с» оказалась кнопкой «Закрыть» в руках владельца, а не багом — инструментация назвала виновника | 2026-08-31 |
| Иконка приложения и уведомлений 0.15.0 (B-31, B-33) | `briefcase update --update-resources` + побайтовый гейт «иконка в бандле — наша»; тест нотифаера больше не выходит за процесс (подменяет `subprocess.run`, проверяет обе ветки — успех и отказ); в `conftest.py` автопредохранитель, который валит тест на TEARDOWN, если он дошёл до настоящего `display notification` | **live-verified** | 400 тестов (+2); ruff clean. ЖИВЬЁМ 2026-08-31: гейт печатает «иконка в бандле: наша (95718 байт)»; `NSWorkspace.iconForFile("/Applications/vibe-bridge.app")` отдаёт наш знак (снято в PNG и просмотрено), `URLForApplicationWithBundleIdentifier` указывает на /Applications; оболочка 0.15.0 нотаризована (`spctl` → accepted); уведомление послано настоящим MCP-вызовом `notify` и доставлено как `me.sshlg.vibe-bridge` (запись в базе Центра уведомлений). Предохранитель проверен ПОДСАЖЕННЫМ дефектом: первая версия (бросок при вызове) дефект пропустила — `_osa` ловит `Exception` и честно отвечает «уведомление не показано»; версия с проверкой на teardown валит его. **Остаток: Центр уведомлений на этой машине всё ещё рисует пчелу — B-36** | 2026-08-31 |
| Живая сверка с Pi (B-30 закрыта, B-27 транспорт) | Ничего не менялось — это сверка уже отгруженного кода против настоящего робота, ради которой B-30 и B-27 держались открытыми | **live-verified** | ЖИВЬЁМ 2026-08-31, робот на build 138, online. `/api/robot/system` → `ok:true`: CPU 48.7 °C (в панели 51.6 через минуту — датчик живой, а не константа), аптайм «2д 22ч 47м», RAM 49.9 %, диск 46.3 %, троттлинг `raw: 0x0` и `throttled_ever: false`, воздух PM2.5 4.2 / PM10 6.9, вентилятор auto, семь сервисов `active+health`, метрики агента (13 ходов за день, задержка 4074 мс). Карточка «Система робота» снята на экране: те же цифры, каждый сервис точкой И словом «работает». Строка троттлинга не показана — и это верно: она рендерится только при `throttled.ok === false`, то есть появляется лишь когда есть о чём сообщить. Панель открыта штатным путём — клик по голове → кнопка «Панель» в компаньоне, компаньон при этом сам закрылся. Медиа-прокси: настоящий кадр с Pi отдан как 200 `image/jpeg` 405 700 байт, валидный JPEG 1640×1232 (содержимое не открывалось — это снимок из дома владельца); отказы честные: 404 на несуществующее имя, 404 на `../../etc/passwd`, 401 без токена, тело `{"error":"файл недоступен"}` без внутренностей. **Последняя миля B-27 — событие с медиа в ленте — требует нового снимка, то есть камеры в доме: спрошено, не сделано** | 2026-08-31 |
| Раскладка панели 0.16.0 (B-29) + время в журнале | Одна сетка `repeat(auto-fill,minmax(288px,1fr))` с `align-items:start` на дашборде и в настройках; `#feed` ограничен по высоте и скроллится сам; `margin-top` снят с карточек — отступ принадлежит `gap`; тело 880→1180; узкие карточки настроек сгруппированы после двух широких. Отдельно: `localTs()` — журнал показывал UTC как настенные часы | **live-verified** | 405 тестов (+5); ruff clean; ux-lint 0 err. ЖИВЬЁМ 2026-08-31 на экране в окне 940: дашборд — четыре карточки в две колонки, каждая своей высоты, лента во всю ширину внизу; пустой колонны во всю высоту, снятой часом раньше, нет. Настройки — таблица способностей во всю ширину (10 способностей, все `доступно`), дальше узкие карточки парами. Время: лента показывала `20:45:32` при часах `22:45:32` — сдвиг ровно на смещение зоны, потому что аудит пишет UTC (`datetime.now(UTC)`), а панель резала ISO-строку и печатала её как местную; после фикса снято `22:50:28` при часах `22:50:43`. Формула проверена в node на четырёх входах — **разово, руками; исполняемого теста нет, только механическая проверка формы (A-39)**: UTC со смещением и с `Z` → местное, мусор и пустое → честная деградация вместо «Invalid Date». Правило `align-items:start` проверено ПОДСАДКОЙ: снятие роняет `test_a_card_is_its_own_height`. Оболочка 0.16.0 нотаризована (`spctl` → accepted) | 2026-08-31 |
| Позиция питомца и три находки под ней 0.17.0 (B-34) | `pet_pos` в state + чистая `clamp_origin`; сообщение `drop` на отпускании (один раз на жест); `wait_for_server` перед постройкой окон; standalone биндит все интерфейсы; наши страницы и скрипты отдаются с `Cache-Control: no-store`; провал сохранения позиции докладывается в журнал | **live-verified** | 421 тест (+16); ruff clean. ЖИВЬЁМ 2026-09-01. Позиция: перетащил голову с (1596, 871) на (1236, 607), `pet_pos` в state стал `[1236.0, 406.0]` (Cocoa-координаты, отсчёт снизу), после ПОЛНОЙ переустановки приложения голова вернулась в (1236, 607). Три находки, каждая ловилась предыдущей: (1) окна загружали страницу до того, как сервер занял порт, и `WKWebView` показывал свою «Нет связи с…» вместо головы, не повторяя попытку — снято на экране; `wait_for_server` немедленно сообщил в журнал «порт 48620 не ответил за 10 с», что и вывело на (2); (2) в режиме standalone `lsof` показал LISTEN только на tailnet-IPv4, а `127.0.0.1:48620` был ЗАКРЫТ, при том что панель, окно приложения и оба окна виджета адресуют мост как `127.0.0.1` — то есть приложение было сломано целиком при живом сервере, и это дефолтный режим новой установки; после фикса loopback открыт, оба окна виджета доложили о подъёме; (3) `drop` не доходил, потому что WKWebView отдавал страницу из своего дискового кэша ЧЕРЕЗ рестарт приложения — питомец исполнял вчерашний JS внутри сегодняшнего приложения; диагноз подтверждён `rm -rf ~/Library/WebKit/<bundle>`, после которого тот же драг сохранился. Заголовок `no-store` проверен живьём на `/mascot` и `/mascot.js`. Оболочка 0.17.0 нотаризована (`spctl` → accepted) | 2026-09-01 |
| A-5 пейринг с карты (+A-30 половина) | `wizard.provision_script` переписан: пейринг делает `scripts/bridge_pair.py` робота (`--status` → «уже спарен?», `--from-file` → пейринг со СВОИМ адресом и creds по пути его юнита), устанавливает `scripts/deploy.sh` вместо дев-пушера `pi-deploy.sh`, поднимает `bridge-api.service`, закрывает себя только после успеха; `/pair` не называет «связан ✓» пейринг без адреса | unit (скрипт ИСПОЛНЯЕТСЯ), live pending | 448 тестов (+7 net), ruff clean. Скрипт с карты впервые запускается набором целиком: `bash` под временным корнем (`VB_PROVISION_ROOT`), `git`/`systemctl`/`sleep`/`shred` — заглушки на PATH, каждый вызов в журнал. Шесть тестов были КРАСНЫМИ до правки (естественная подсадка: старый скрипт слал `curl {token,name}`). Исполнение поймало собственный дефект правки: `install -D` — GNU-only, на BSD падал, а `shred` оригинала шёл следующей строкой безусловно — единственный токен пейринга исчезал бы совсем; теперь `&&`. **Живая проверка требует физической карты и Pi — B-9/A-30 остаток** | 2026-09-02 |
| A-6 неповторяемый ход чата (ADR-0007) | `RobotClient.chat` повторяет только недозвон; `api_robot_chat` держит один ход на сессию (409) | unit | 450 тестов (+2 net), ruff clean. Два теста были красными до правки: `ReadTimeout` давал ДВА хода к мозгу. Гвард одновременности проверен ПОДСАЖЕННЫМ дефектом — с `if False and …` тест краснеет, без него зелёный. Асинхронный транспорт в тесте нужен по существу: блокирующий обработчик подвесил бы цикл и доказывал бы устройство TestClient, а не гвард. **Живьём против настоящего мозга не проверялось: нужен ход, который реально дольше 150 с** | 2026-09-02 |
| A-7 канал ответа робота | Робот: `~/.robot_bridge_turn.json` со сроком хода вместо записи в телеграмный файл; `_context_route_chat_id` → `int \| None`; обе лестницы маршрутизации деградируют словами | unit (репозиторий робота), live pending | **Заимствованное свидетельство: коммит `595c41f` в репозитории робота, из моста не проверяется.** 2123 теста робота зелёные, ruff clean. `KeyError` воспроизведён тестом ДО правки (`int(ctx["chat_id"])`, `io.py:762`). Тест шва переписан с grep по исходнику на сквозной через два модуля; проверка «мост отметил ход» перенесена ВНУТРЬ хода — после ответа ход закрыт, и прежняя проверка не доказывала ничего. **Живьём против Pi не проверялось: нужен ход с tool-call'ом и телеграм-сообщение в его середине** | 2026-09-02 |
| Релиз 0.20.0 (волна 1 аудита: A-5, A-6, A-7) | Только payload — сторонних зависимостей не добавлялось, оболочка не пересобиралась (правило `docs/spec/packaging.md`) | **live-verified** | 450 тестов, ruff clean, ux-lint 0 ошибок. ЖИВЬЁМ 2026-09-02: payload 122 416 байт подписан, самопроверка подписи ok; `releases.atom` отдаёт `v0.20.0` первым; работающий мост 0.19.0 по `/api/update/check` нашёл, скачал, проверил подпись и поставил — `pending: 0.20.0`; после перезапуска `running 0.20.0 | source payload`. **Гвард A-6 проверен против НАСТОЯЩЕГО робота** (Вася, build 142, online): два хода в одну сессию с разрывом 0.7 с — первый вернул ответ мозга «Привет.» (200), второй 409 «ход уже идёт», то есть мозг увидел ОДИН ход. Пойман живьём побочный факт: `open -a` не поднимает приложение, пока предыдущий экземпляр гасится — прямой запуск бинаря поднимает; в ленту это не попадает, потому что процесса ещё нет (B-41) | 2026-09-02 |
| A-8 поимённый грант | `ConsentEngine._grant_until` по имени инструмента; `grants()` перечисляет живые; снимок отдаёт `grants[]` с человеческим именем; кнопки трёх поверхностей и строка настроек переписаны; SCN-003 приведён в правду | unit, live pending | 453 теста (+3 net), ruff clean, ux-lint 0 ошибок. Три теста были красными до правки. Утечка на соседа доказана прямо: грант на `open_url`, затем `automation` — раньше `AUTO`, теперь `TIMEOUT` (отвечать некому). Два теста набора закрепляли СТАРУЮ семантику и переписаны: `test_grant_suppresses_second_dialog` проверял, что грант на `mac_open_app` покрывает `mac_open_url`, а `test_a_grant_covers_later_reads_like_any_other_class` — что снимок экрана покрывает список приложений. **Живьём не проверялось: нужен ACT-вызов с гранта и соседний вызов следом** | 2026-09-02 |
| A-9 + V-3 срок вопроса виден | `remaining()`/`ask_timeout_s` в движке; `left_s`/`timeout_s` в обоих снимках; таймер-бар на трёх поверхностях; модальный лист называет окно словом; `refusal_text` берёт настоящее число | unit, live pending | 458 тестов (+5), ruff clean, ux-lint 0 ошибок. Тесты поверхностей смотрят на ПРАВИЛА с вырезанными комментариями — иначе зачли бы объяснение вместо кода (класс A-32). Побочно поймано и починено: `test_the_pet_breathes_but_stops_when_asked` читал ТОЛЬКО первый блок `prefers-reduced-motion`, и появление второго покрасило его на файле, где гашение анимации не менялось; теперь читаются все блоки. Побочно закрыт хвост A-8: окно-компаньон `mascot.html` несло старую кнопку «На 15 мин» — единственная из четырёх поверхностей, которую первая правка не тронула. **Живьём не проверялось: нужен ACT-запрос и наблюдение за полосой** | 2026-09-02 |
| A-10 опоздавший ответ | Запрос закрывается по таймауту; `Outcome` и кольцо последних 32 исходов; панель, системный лист и питомец называют ПРИЧИНУ | unit, live pending | 463 теста (+5), ruff clean, ux-lint 0 ошибок. Тест был красным до правки: `resolve()` после таймаута возвращал True. Гонка проверена отдельно — ответ в зазоре побеждает закрытие. Побочно закрыт проглот у питомца: ветка отправки решения была `catch(e){}` и съедала обе новости; проверка теперь требует, чтобы недоступный мост был НАЗВАН, а не «нет пустых catch» — пустой catch вокруг разбора JSON законен, там запасная строка уже стоит. **Живьём не проверялось: нужен ACT-запрос, минута молчания и клик после** | 2026-09-02 |
| A-11 живая карта способностей | `AvailabilityMap` с TTL и принудительным обновлением; `request_permission` (системный диалог → панель настроек); `/api/capabilities/{name}/grant`; кнопка в таблице; уведомление владельцу с гашением повторов | unit, live pending | 471 тест (+8), ruff clean, ux-lint 0 ошибок. Три теста карты были красными до правки. Проверено, что TTL действительно гасит burst (счётчик опросов) и что `refresh()` его игнорирует. Отдельно закреплено, что «нет команды» правами не лечится и уведомления не вызывает, и что упавший нотифаер не стоит роботу ответа. **Живьём не проверялось: нужен отзыв прав записи экрана в системных настройках и вызов `screenshot` роботом** | 2026-09-02 |
| Релиз 0.21.0 (волна 2: A-8, A-9+V-3, A-10, A-11) | Только payload — сторонних зависимостей не добавлялось | **live-verified частично** | 471 тест, ruff clean, ux-lint 0 ошибок. ЖИВЬЁМ 2026-09-02: payload 129 575 байт подписан, самопроверка ok; мост 0.20.0 сам нашёл релиз, проверил подпись, поставил; после перезапуска `running 0.21.0 | source payload`. Живая карта способностей отдаёт все 10 через `AvailabilityMap` (не замороженный словарь). **Кнопка «Выдать права» проверена настоящим системным вызовом**: `POST /api/capabilities/screenshot/grant` → `CGRequestScreenCaptureAccess` вернул True, ответ «права записи экрана выданы», свежая карта из 10 строк пришла тем же ответом. Ветка «macOS второй раз диалог не покажет → открываем панель настроек» живьём НЕ проверена: для этого надо отозвать права у бандла. Таймер-бар и поимённый грант живьём не наблюдались — нужен ACT-запрос от робота | 2026-09-02 |
| A-12 честная ручная привязка | `RobotClient.probe()` с четырьмя исходами; `/api/robot/attach` отдаёт `reached` и `probe.kind`; форма принимает `chat_url` и печатает причину | unit, live pending | 481 тест (+10), ruff clean, ux-lint 0 ошибок. Пять тестов probe были красными до правки. Отдельно закреплён случай «чужой сайт отвечает 200 HTML» — раньше он проходил как робот, потому что валидировался только префикс `http(s)://`. **Живьём не проверялось: настоящий робот уже привязан визардом, и перепривязка вручную сожгла бы рабочую связку** | 2026-09-02 |
| A-13 + A-18 одно событие — одна строка | Консьюмер событий зовёт `_base_notify`; `channel` доносится до ленты и помечается | unit, live pending | 483 теста (+2), ruff clean, ux-lint 0 ошибок. Тест был красным до правки и показал дефект дословно: две строки, вторая `{'kind': 'notify', 'text': 'Вася: полил цветы'}` — без медиа. Питомец считается шпионом на `Mascot.say`: удвоение там невидимо по состоянию (вторая фраза просто перетирает первую тем же текстом), и только счётчик вызовов его ловит. **Живьём не проверялось: нужно проактивное событие от робота** | 2026-09-02 |
| A-14 уборка каталога медиа | Робот: `_prune_bridge_media` (возраст + объём, самое старое первым), уникальное имя кадра, fail-open отдельно от съёмки | unit (репозиторий робота), live pending | **Заимствованное свидетельство: коммит `582cb99` в репозитории робота, из моста не проверяется.** 2165 тестов робота зелёные, ruff clean. Четыре теста были красными до правки; столкновение имён воспроизведено прямо — два `_stage_for_bridge` подряд давали ОДИН файл. Отдельно закреплено, что падение уборки не стоит владельцу кадра. **Живьём не проверялось: нужен снимок с камеры и наблюдение за каталогом на Pi** | 2026-09-02 |
| A-15 гейт wire-парности | `[tool.vibebridge.wire]` как единственный источник; `build_app.sh` читает пин; `tests/test_wire_parity.py` — три проверки | unit + подсадка | 486 тестов (+3), ruff clean, `bash -n` на скрипте сборки. Все три были красными до правки. Кросс-репозиторный гейт проверен ПОДСАДКОЙ: `hermes = "0.20.0"` → «мост собран под Hermes 0.20.0, робот пинит 0.19.0». Измерено на этой машине: `HERMES_VERSION=0.19.0` в `requirements/orchestrators.env` робота. **Скрипт сборки с новым чтением пина живьём не гонялся — следующая сборка оболочки его и проверит; сейчас релизы payload-only** | 2026-09-02 |
| A-16 + A-21 числа берутся из настроек | `start_autoupdate(…, settings)`; веб-слой без константы порта; механический гейт «в `web.py` нет `BRIDGE_PORT` и нет 48620» | unit, live pending | 490 тестов (+4), ruff clean, ux-lint 0 ошибок. Три теста были красными до правки. Порт проверен на нестандартном значении (51234) сразу на трёх поверхностях — карточка телефона, старт визарда, `mcp_url` в ответе `/pair`. **Живьём не проверялось: нужен запуск моста на порту, отличном от умолчания** | 2026-09-02 |
| A-17 снимок под потолком | `sips`-масштаб + JPEG; `encode_screenshot` как общий потолок для трёх паков; `clamp_width`; гейт против собственного кодировщика в паке | unit, live pending | 495 тестов (+5), ruff clean. Три теста были красными до правки. Проверены обе ветки macOS — удачный `sips` (JPEG) и сломанный (честная деградация в PNG). Старый тест A-3 обновлён: файлов теперь ДВА, и он требует, чтобы ушли оба. **ИЗМЕРЕНО на этой машине 2026-09-02** настоящим `screencapture` + `sips`: полноэкранный PNG 1 237 357 байт → data-URL 172 243 байт, в 9,8 раза меньше. Старое поведение на ЭТОМ экране отдало бы ~1,7 МБ текста за снимок, то есть уже перешагивало новый потолок; на 5К было бы кратно хуже. Через MCP роботом не вызывалось | 2026-09-02 |
| Релиз 0.22.0 (A-12, A-13+A-18, A-14, A-15, A-16+A-21, A-17) | Только payload | **live-verified** | 495 тестов, ruff clean, ux-lint 0 ошибок. ЖИВЬЁМ 2026-09-02: payload 132 300 байт подписан; мост 0.21.0 сам нашёл релиз и поставил; после перезапуска `running 0.22.0 | source payload`; `/api/phone` отдаёт команду с ДЕЙСТВУЮЩИМ портом (48620 здесь и есть действующий — совпадение с умолчанием, поэтому доказательство слабое, и сильное лежит в тесте на 51234). Гейт парности версий поймал мой собственный промах при бампе: `[tool.vibebridge.wire]` сдвинула строку, и `sed` по номеру строки бампнул не ту версию — оболочка и payload разъехались бы молча | 2026-09-02 |
| A-19 лента переживает перезапуск | Мост: `EventFeed` (диск, потолок, дедуп, загрузка при старте), граница разговора вместо стирания. Робот: досылка хвоста при подключении | unit, live pending | 502 теста моста (+7) и 2175 робота (+2), ruff clean обеих сторон. Дедупликация проверена и ПОСЛЕ перезапуска — без этого первый же реконнект удвоил бы ленту. Отдельно закреплено: битая строка в файле не стоит остальных, а отказ диска докладывается (`last_error` → журнал), а не глотается. Два теста закрепляли старый контракт и переписаны: один требовал пустую ленту после нового разговора, второй — что хвост НЕ досылается. **Живьём не проверялось: нужно выключить мост, дождаться события робота и включить обратно** | 2026-09-02 |
| A-20 медиа под потолком и с именем беды | Потоковое чтение с потолком; дешёвый отказ по `Content-Length`; пять различимых исходов; статусы в эндпоинте | unit, live pending | 507 тестов (+5), ruff clean. Потолок проверен обеими ветками — превышение по факту и по объявленному размеру. Два старых теста читали `tuple | None` и переписаны. Побочно измерено: голый `..` в пути до обработчика не доходит — роутер нормализует путь, это его собственная защита, и тест проверяет то, что ДОХОДИТ (`a..b`). **Живьём не проверялось: настоящий кадр с Pi проходил 2026-08-31 как 405 700 байт, то есть в потолок укладывается с запасом; отказ по размеру требует большого файла на роботе** | 2026-09-02 |
| A-22 срок и ротация пейринг-токена | Мост: `pending_pair_token_at`, `consent.pairing_ttl_hours`, свежий `robot_token` на каждый пейринг. Робот: проба записи до предъявления токена | unit, live pending | 510 тестов моста (+3) и 2177 робота (+2), ruff clean обеих сторон. Три теста были красными до правки. Ротация проверена прямо: два пейринга подряд дают РАЗНЫЕ постоянные ключи. **Живьём не проверялось намеренно: перепейринг сжёг бы рабочую связку с настоящим роботом** | 2026-09-02 |
