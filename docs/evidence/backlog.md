# Board — vibe-bridge

Seeded 2026-08-29 by the v2 design run (first pipeline run of this repo).
Priorities re-derived at each run's close-out.

| ID | Priority | Title | Source | Status |
|---|---|---|---|---|
| B-1 | P1 | Build: T-WS/CORE/NORTH/PANEL/SOUTH/PHONE/WIZARD-a/PLATFORM-паки **shipped**; live-проверка Win/Linux паков и трея — нужны машины | brief-2026-08-29-build | packs done, live pending |
| B-9 | P2 | WIZARD-b: скачивание+запись образа с элевацией (authopen/UAC/pkexec) + степпер SCR-06 целиком; live с физической картой | build-plan M-WIZARD | open |
| B-8 | P1 | Human: ~~Serve включён, HTTPS live~~ → остался телефон: открыть phone_link из настроек панели, iPhone «На экран Домой», «Включить пуши» | T-PHONE 2026-08-29 | half-done |
| B-2 | P1 | ~~Robot-side bridge-API~~ **ЗАКРЫТО 2026-08-29**: смержено в fleet-ветку робота (a2348d9), после merge проверено живьём (v1.1.0+181 build 108, чат отвечает) | brief-2026-08-29 CO-4 | done |
| B-3 | P2 | ~~Переименование → vibe-bridge~~ **ЗАКРЫТО 2026-08-30**: пакет/репо/машина/плист, миграция state+логов (пейринг цел) | brief-2026-08-29 CO-1 | done |
| B-4 | P2 | ~~macOS-нога~~ **ЗАКРЫТА 2026-08-30**: подписанный .app, оболочка+payload, автообновление и автозапуск — живьём. Осталась Windows/Linux-нога: нужны машины и сертификаты | brief-2026-08-29 CO-5 | macOS done, Win/Linux pending |
| B-5 | P3 | `/brand-init` + copywriting панели/визарда | brief-2026-08-29 CO-3 | open |
| B-6 | P3 | Figma-визуальный проход по wireframes | brief-2026-08-29 CO-2 | open |
| B-7 | P3 | Снять `mac_*`-алиасы инструментов — вместе с бампом Hermes/`mcp` | spec §5, close-out 2026-08-29 | open |
| B-10 | P2 | Приватный Ed25519-ключ релизов существует в одном экземпляре (keychain этой машины) — нужна процедура бэкапа и ротации; потеря = невозможность выпускать обновления | brief-2026-08-30 CO-4 | open |
| B-11 | P1 | ~~Нотаризация macOS-сборки~~ **ЗАКРЫТА 2026-08-30**: профиль заведён оператором, `--notarize` прошёл (Accepted), staple на .app и .dmg, `spctl` → accepted; DMG в релизе `shell-v0.1.0` | brief-2026-08-30 REQ-10 | done |
| B-12 | P3 | Лицензия репозитория: сейчас `LicenseRef-Proprietary` (все права сохранены) — решить, открывать ли под MIT, как остальная семья | brief-2026-08-30 | open |
| B-18 | P2 | ~~Порт занят~~ **ЗАКРЫТО 2026-08-31**: правда за кодом — порт это контракт с роботом; спека исправлена, сообщение называет держателя порта и путь смены | анализ 2026-08-30 | done |
| B-19 | P3 | ~~Репозиторий робота зашит~~ **ЗАКРЫТО 2026-08-31**: `robot.repo` в config.toml, панель передаёт его визарду | анализ 2026-08-30 | done |
| B-20 | P3 | ~~Согласие на READ~~ **ЗАКРЫТО 2026-08-31**: умолчание оставлено визийным (READ виден в журнале сразу после), добавлен `consent.ask_for_read` и кнопка в панели; проверено живьём | анализ 2026-08-30 | done |
| B-13 | P3 | ~~Иконка приложения~~ **ЗАКРЫТА 2026-08-30**: знак нарисован кодом из токенов пака workbench (висячий мост), `.icns` + плитки PWA, регенерируется каждой сборкой | brief-2026-08-30 | done |
| B-14 | P2 | Побочный приватный репозиторий `sshlg/vibe-bridge` создан `gh repo create` по ошибке; удалить (у токена нет scope `delete_repo` — шаг оператора) | brief-2026-08-30 | human step |
| B-15 | P2 | App-specific password для notarytool засветился в транскрипте сессии — отозвать на appleid.apple.com и, если понадобится, завести профиль заново (сам профиль в keychain останется рабочим до отзыва) | build-3 close-out | human step |
| B-16 | P3 | Иконка нарисована агентом по токенам пака, но через `/sheleg-design` не проходила — визуальный проход у знака бренда стоит сделать вместе с B-6 (Figma) | build-3 | open |
| B-17 | P2 | Intel-Маки не поддерживаются: `universal_build = false`, потому что `rumps` и `http-ece` издаются только исходниками. Чтобы поддержать — собрать колёса самим и подложить через `--find-links` | build-3 | open |
| B-21 | P3 | Пауза и строгий режим READ: при `ask_for_read` пауза глотает READ целиком (принцип 3) — проверено юнитом, живьём не гонялось | build-5 | open |
| B-22 | P3 | Питомец: пока одна поза и мимика состояниями. Характер, покачивание и деградация движения до покоя — через `/sheleg-design` вместе с B-16 | build-6 | open |
| B-23 | P3 | Питомец только на macOS (NSPanel+WKWebView). Win/Linux — окно поверх экрана средствами pystray/Qt либо остаётся только панель | build-6 | open |
| B-24 | P3 | Шорткаты клавиатурой (глобальный хоткей на «сказать роботу») — обсуждалось, не сделано: нужен отдельный TCC-грант и решение, какой хоткей не конфликтует | build-7 | open |
| B-25 | P3 | Прозрачное окно питомца перехватывает клики в пределах своей рамки; сейчас рамка ужата до фигурки, но точную маску по силуэту не делали | build-7 | open |
| B-26 | P1 | ~~Статус системы в панели~~ **ОБЕ СТОРОНЫ СДЕЛАНЫ 2026-08-31**: робот — `/bridge/system` (коммит ebaf994, 23 теста, CI зелёный); мост — `RobotClient.system()`, `/api/robot/system`, карточка в панели. Живая сверка ждёт, пока робот подхватит коммит своим таймером | build-8/9 | ждёт апдейта робота |
| B-27 | P3 | ~~Вложения от робота~~ **ТРАНСПОРТ ПРОВЕРЕН ЖИВЬЁМ build-15**: прокси `/api/robot/media/<имя>` отдал настоящий кадр с Pi — 200, `image/jpeg`, 405 700 байт, валидный JPEG 1640×1232, страница токен робота не держит; отказные ветки честные (404 на несуществующее и на обход каталога, 401 без токена, тело без утечек). Осталась последняя миля: событие с медиа в ленте виджета — нужен НОВЫЙ снимок (робот подтвердил: инструмента «отправить существующий файл» нет), то есть камера и LED-вспышка в доме владельца — спрошено | build-8/10 | ждёт разрешения на снимок |
| B-28 | P2 | Скины маскота и анимации стадий — сейчас одна поза и мимика состояниями; через `/sheleg-design` вместе с B-16/B-22 | build-8 | open |
| B-29 | P2 | Панель (дашборд/настройки) не переработана по дизайну. **Закрыта build-16**: жёсткий сплит `280px 1fr` заменён самораскладывающейся сеткой с `align-items:start` (растяжение элемента под высоту ряда и было пустой колонной во всю высоту), тело подняли 880→1180, лента ограничена и скроллится сама, отступы отданы `gap`, узкие карточки настроек сгруппированы. Четыре правила держатся тестами, проверенными подсадкой | build-8 | **closed** |
| B-30 | P2 | Живая сверка карточки «Система робота» против настоящей Pi. **Закрыта build-15**: робот дошёл до build 138, `/api/robot/system` отдал `ok:true` с настоящими цифрами (CPU 48.7→51.6 °C, аптайм 2д 22ч 47м, RAM 49.9 %, диск 46.3 %, троттлинг `0x0` — не было ни разу, воздух PM2.5 4.2 / PM10 6.9, семь сервисов active+health); карточка в панели снята на экране и показывает их же, статус сервисов несёт и точку И слово | build-9 | **closed** |
| B-31 | P2 | Иконка уведомления — пчела. **Закрыта build-15: пчела была НАШЕЙ иконкой.** macOS показывает значок публикующего бандла, а `briefcase create` ставит ресурсы ПОСЛЕ установки зависимостей — шаг, который у нас всегда падает и глушится `|| true`, — так что иконка не копировалась ни разу за 14 релизов. Фикс: `briefcase update --update-resources` + побайтовый гейт в сборке | build-10/13 | **closed** |
| B-32 | P2 | Инструменты робота «отправить в канал» и «показать информацию из канала» — просил владелец; сейчас есть только target='computer' и телеграм-маршруты | build-10 | open |
| B-33 | P2 | Уведомления «заголовок/текст» с иконкой Script Editor. **Закрыта build-15: это был НАШ тест.** `test_notifier_never_raises` прятал `desktop_notifier`, чтобы дойти до osascript-ветки, и вызывал нотифаер по-настоящему — каждый прогон набора, а сборка гоняет его первым шагом, ставил владельцу настоящий тост от имени Script Editor. Поэтому в журнале моста записей и не было, и поэтому я дважды сказал «это не мы». Фикс: тест подменяет `subprocess.run`; в conftest автопредохранитель, проверенный подсаженным дефектом | build-13 | **closed** |
| B-34 | P3 | Позиция питомца не переживает перезапуск. **Закрыта build-17**: `pet_pos` в state, пишется один раз на жест (сообщение `drop` по отпусканию, не по каждому пикселю), восстанавливается через чистую `clamp_origin` — позиция, сохранённая на втором мониторе, подтягивается на существующий экран. Живьём: перетащил, переустановил приложение целиком, голова вернулась в (1236, 607) | build-14 | **closed** |
| B-35 | P3 | Окно-компаньон висит на экране всегда (альфа 0, мышь сквозь) — это плата за то, что его таймеры не душатся. Проверить, не стоит ли вместо альфы держать его размером 1×1 в углу, когда нечего показывать | build-14 | open |
| B-36 | P3 | На ЭТОЙ машине Центр уведомлений всё ещё рисует старую иконку (пчелу) для `me.sshlg.vibe-bridge`, хотя бандл несёт наш знак и `NSWorkspace.iconForFile` его отдаёт. Проверено: уведомление в базе не несёт иконки вовсе (ключи `req`: dest/body/cate/iden/titl) — значок берёт система по bundle id. Вычищены кэши LaunchServices и iconservices, перезапущены `usernoted` и `iconservicesagent` — без изменений. Остаток истории 14 релизов с чужой иконкой; на чистой установке такой истории нет. Пере-проверить после logout | build-15 | open |
| B-37 | P2 | Решение по биндингу в режиме standalone. build-17 поставил `0.0.0.0`, потому что tailnet-only бинд ломал приложение целиком (панель и оба окна виджета идут на `127.0.0.1`). Граница — не бинд, а host-allowlist (421 на чужой Host) плюс bearer на `/mcp`, но LAN-сканер теперь видит открытый порт. Альтернативы: остаться в `gateway` (только loopback), если робот не должен достучаться до моста напрямую; либо перенести внешний доступ на `tailscale serve` — уже документированный путь для телефона — и биндить только loopback. Обе меняют механику пейринга, поэтому выбор за владельцем | build-17 | open |
| B-38 | P2 | Инсталлер робота на свежей плате не знает, чей это дом: `scripts/deploy.sh` резолвит `ROBOT_USER` из `operator.linux_user` → `SUDO_USER` → `USER` → `sshlg`, а из provision-юнита он идёт от root без `SUDO_USER` — на карте, залитой Imager'ом с ЛЮБЫМ другим именем пользователя, юниты отрендерятся на несуществующий `/home/sshlg`. Найдено при закрытии A-5; чинится в репозитории робота | A-5 close-out 2026-09-02 | open |
| B-39 | P3 | В репозитории робота три готовых набора инвариантов лежат НЕОТСЛЕЖИВАЕМЫМИ и не заведены в `ci.yml` (`test_invariant_relay_gpio17`, `test_invariant_shell_escape`, `test_invariant_sudoers`; 21 тест, все зелёные) — гейт `test_ci_wiring` красный на чистом дереве. Не мои файлы, поэтому не коммичу молча | A-7 close-out 2026-09-02 | **закрыто** 2026-09-02 не мной: параллельная сессия в репозитории робота завела инварианты в `ci.yml` и закоммитила их — `test_ci_wiring` зелёный на чистом дереве (проверено) |
| B-40 | P2 | Канал ответа робота разводится по времени, а не по идентичности хода: телеграм-сообщение, начатое ВНУТРИ живого 150-секундного хода моста, разрешится в компьютер. Развести можно только идентичностью хода (её нет в протоколе инструментов) либо симметричным лизом на телеграмной стороне. Условие пересмотра названо в коде | A-7 close-out 2026-09-02 | open |
| B-41 | P3 | `open -a /Applications/vibe-bridge.app` молча не поднимает приложение, пока гасится предыдущий экземпляр: два вызова подряд не дали процесса, прямой запуск бинаря дал сразу. Владелец в этот момент видит «ничего не произошло» и жмёт ещё раз; в журнал не попадает ничего — процесса нет. Измерено при выкатке 0.20.0 | 0.20.0 close-out 2026-09-02 | open |

---

# Аудит 2026-09-01 — находки и план

Аудит запускался семью субагентами, каждому свой угол. **Пять из семи упали на
внешнем ограничении** (`individual spend limit`), поэтому доказательную базу
имеют только три угла: юзабилити (агент), шов робот↔мост (агент) и
логика/границы безопасности (проверено мной вручную). **НЕ проаудированы:**
архитектура кода, визуальный слой, соответствие сценариям/JTBD, качество
тестов. Это не «там всё хорошо» — это «там не смотрели».

Строки `A-NN` — находки аудита. Severity в колонке приоритета: P0 значит
«исправлять до следующего релиза».

| ID | Приоритет | Находка и доказательство | Статус |
|---|---|---|---|
| A-1 | P0 | **`automation` выдавал shell, который продукт отрицает.** `do shell script` отсутствовал в `APPLESCRIPT_BLOCKED` (`vibebridge/capabilities.py:26`), а последняя запись содержала `»` вместо `to` — `system events» keystroke` не совпадала ни с одним настоящим скриптом, то есть синтез нажатий не блокировался никогда. Прямое нарушение анти-визии «Никакого shell» и докстроки самого модуля. Проверено прогоном настоящих строк через список | **закрыто** build-18: блоклист расширен (`do shell script`, `keystroke`, `key code`, `system events`), в коде сказано вслух, что подстрочный блоклист — не песочница; 2 теста настоящими скриптами |
| A-2 | P0 | **`/mcp` без аутентификации на свежей установке.** `BearerGuard` читал «токен есть → проверить», поэтому в standalone до пейринга пропускал всё (`web.py:117`). Единственной «границей» оставался allowlist по `Host` — а он не граница: `Host` присылает клиент, достаточно назвать `127.0.0.1`. Измерено на этой машине: чужой Host даёт 401 от bearer, но при отсутствии токена проверять было нечему | **закрыто** build-18: до пейринга `/mcp` отвечает 401 `unpaired`; добавлен `PeerGuard` — граница по адресу пира (loopback + CGNAT тейлнета), взводится когда бинд шире loopback; 5 тестов |
| A-3 | P0 | **Снимки экрана владельца оставались на диске навсегда.** `tempfile.mktemp()` + чтение без удаления (`capabilities.py:98`), при том что linux-пак рядом удаляет корректно | **закрыто** build-18: `finally: os.unlink`, тест проверяет отсутствие файла после вызова |
| A-4 | P0 | **Три строки карточки «Система робота» не рисовались никогда, и B-30 был закрыт неверно.** Робот отдаёт `uptime` СТРОКОЙ (`dashboard_data.py:433`), панель читала `d.uptime.pretty/.load1/.cpu_pct` (`index.html:622`); `cpu_pct` и `load` вообще не проецировались (`bridge_api.py:245`). Фикстура робота мокала форму, которой сборщик не производит (`test_bridge_api.py:330`) — поэтому зелёный ничего не значил. Мой скриншот карточки это подтверждал: я сверил, что показанное правда, но не сверил со списком ожидаемого | **закрыто** build-18: проекция расширена, панель терпит обе формы, фикстура приведена к настоящей форме, добавлены утверждения против настоящего сборщика |
| A-5 | P0 | **SD-визард никогда не приводит к «робот подключён».** Скрипт, который мост сам пишет на карту, POST'ит в `/pair` только `{token, name}` (`wizard.py:118`), а `robot_base_url` ставится только при наличии `base_url` (`web.py:519`). Владелец получает уведомление «связан ✓», а панель говорит «не подключён», чат остаётся серым, шаг онбординга не закрывается. Докстрока визарда объявляет протокол `{token, name, base_url, chat_url}` — код ей не соответствует | **закрыто** build-20: пейринг делает `scripts/bridge_pair.py` робота (`--from-file`), он же знает свой адрес и пишет creds туда, где их ждёт его юнит; провижининг ставит `scripts/deploy.sh`, а не дев-пушер `pi-deploy.sh`, и поднимает `bridge-api.service`. Мост больше не называет «связан ✓» пейринг без адреса. Скрипт с карты теперь ИСПОЛНЯЕТСЯ набором целиком (6 тестов, `tests/test_wizard.py:60-160`) |
| A-6 | P0 | **Повтор чата исполняет побочные эффекты дважды.** `robot.py:120` — `for attempt in (1, 2)` с тем же payload после таймаута 150 с; ключа идемпотентности нет ни на ходе, ни в прокси робота (`bridge_api.py:521`). Ход №1 к этому моменту может исполнять tool-call'ы: пост в канал, вспышка и снимок, открытая вкладка. Всё это повторяется | **закрыто** build-20 (ADR-0007): повтор остался только на недозвоне (`ConnectTimeout`/`PoolTimeout` — ход не покинул мост), `ReadTimeout` не повторяется никогда; плюс гвард одновременности — второй ход в одной сессии получает 409 «ход уже идёт» (панель на Маке и на телефоне это одна сессия `panel`, клиентский `chatBusy` про соседа не знает). Ключ идемпотентности сознательно НЕ вводился — обоснование в ADR-0007. Гвард проверен подсадкой |
| A-7 | P0 | **Мост перетирает telegram-контекст робота и может уронить его инструмент.** `bridge_api.py:285` пишет `{channel, ts}` в тот же файл, куда бот пишет с `chat_id`; `io.py:759` заканчивается `int(ctx["chat_id"])` → `KeyError`, когда последним писал мост. Плюс телеграм-сообщение внутри 150-секундного окна возвращает маршрут в телеграм — ровно та жалоба, из-за которой появился `target='computer'` | **закрыто** 2026-09-02 в репозитории робота (`595c41f`, ветка `feat/agent-first-os`): у моста свой файл `~/.robot_bridge_turn.json`, и он значит не «последним говорил мост», а «идёт ход, принятый мостом» — со своим сроком, открытие перед `hermes_post`, закрытие в `finally`. `_context_route_chat_id` возвращает `None` вместо броска, обе лестницы деградируют словами. Тест шва переписан с grep на сквозной (пишет `bridge_api`, читает `io`). 2123 теста робота зелёные. **Предел записан прямо: телеграмный ход, начатый ВНУТРИ живого хода моста, разрешится в компьютер — B-40** |
| A-8 | P1 | **Грант «на 15 мин» выдаётся на КЛАСС, а кнопка об этом не говорит.** `consent.py:110` ключует грант на `tool_class`; «да» на «открыть ссылку» на 15 минут молча включает `automation`, буфер обмена и Shortcuts. Робот об этом не знает: его skill говорит «ACT требует согласия» и про стоящий грант не упоминает | **закрыто** build-21: грант ключуется на ИНСТРУМЕНТ, а не на класс — «да» на «открыть ссылку» больше не включает AppleScript, буфер и Shortcuts. Кнопки на трёх поверхностях говорят «такие», панель называет стоящие гранты человеческим именем из той же строки согласия (второго словаря имён не заводится), снимок отдаёт `grants[]`. Канон SCN-003 переписан тем же изменением (шаг 3 — сосед спрашивается заново). Скил робота называет правило (`7df994a`). Сверка с визией §1 «поимённое согласие» — правка идёт К визии, класс-грант был «всё или ничего» внутри класса |
| A-9 | P1 | **Ни одна поверхность согласия не говорит, что запрос истекает**, а отказ по молчанию — политика по умолчанию (`consent.py:42`). Нет отсчёта ни в панели, ни у питомца, ни в системном диалоге | **закрыто** build-21: `ConsentEngine.remaining()` и `ask_timeout_s`; снимок панели и снимок питомца несут `left_s`/`timeout_s`; таймер-бар построен по паку (4px, заливка `--warn`, дренаж слева направо, линейно) на ТРЁХ поверхностях — панель, окно-компаньон, встроенный питомец; системный лист тикать не умеет и потому говорит словом «Без ответа за N с — отказ». При `prefers-reduced-motion` полоса скрыта, остаётся цифра — как задано паком |
| A-10 | P1 | **Клик «Разрешить» после таймаута ничего не делает и молчит.** По таймауту запрос удаляется из `_pending`, но `_event` не выставляется (`consent.py:103`); `resolve()` возвращает True, ветка «уже решено» не срабатывает (`app.py:255`), а модальный `rumps.alert` сам не закрывается | **закрыто** build-21: по таймауту запрос ЗАКРЫВАЕТСЯ (`resolve(TIMEOUT, by='timeout')`), а не просто убирается из очереди — запоздавший `resolve()` теперь честно возвращает False. Гонка в зазоре решена тем же first-wins: ответ, попавший ДО срока, устоит. Движок помнит исходы последних 32 запросов (`outcome()`), поэтому три поверхности различают «истёк — робот получил отказ по молчанию» и «решён с другой поверхности». Модальный лист сам не закроется (платформа не умеет), но клик по нему через минуту больше не молчит. У питомца ветка отправки была `catch(e){}` — она съедала и истечение, и недоступный мост |
| A-11 | P1 | **Карта способностей врёт до перезапуска, и кнопки выдачи прав нет.** `probe_availability` вызывается один раз (`web.py:298`) и служит источником и для панели, и для мгновенного отказа роботу. Владелец выдаёт «Запись экрана» — ничего не меняется. SCN-020 обещает уведомление и кнопку в системный диалог; в коде нет ни того, ни другого | **закрыто** build-21: `AvailabilityMap` пере-опрашивает систему сама (TTL 5 с — burst из двадцати вызовов не дёргает ОС двадцать раз), и её читают ОБА потребителя: таблица в панели и мгновенный отказ роботу. `request_permission` ведёт к системному диалогу (`CGRequestScreenCaptureAccess`) и, когда macOS второй раз его не покажет, открывает нужную панель настроек. `POST /api/capabilities/{name}/grant` + кнопка «Выдать права» в таблице; после неё карта обновляется принудительно, без ожидания TTL. Уведомление владельцу в момент отказа по правам — раз в 10 минут на способность. SCN-020 из «Coverage: none yet» приведён в правду |
| A-12 | P1 | **Ручная привязка объявляет успех, ничего не проверив.** Валидируется только префикс `http(s)://` (`web.py:695`), затем «привязан ✓». Пустой ключ подставляет свежий `robot_token`, которого робот не видел → 401. `chat_url` в форме нет вовсе (`index.html:395`), поэтому вкладка «Чат» включается, а чат отвечает «робот не подключён» рядом с карточкой «в сети» | **закрыто** build-22: `RobotClient.probe()` дозванивается и РАЗЛИЧАЕТ четыре исхода — отвечает / ключ не подошёл (401·403) / по адресу не робот (нет карточки статуса) / не дозвонились. Привязка сохраняет настройку, но успехом её не называет: журнал, уведомление и форма говорят, что именно не так, и подсказывают следующий шаг. В форме появился `chat_url` — без него вкладка «Чат» включалась, а чат отвечал «робот не подключён» рядом с карточкой «в сети» |
| A-13 | P1 | **Каждое событие робота попадает в ленту дважды**, и питомец произносит его дважды: `web.py:1073` пишет событие, `web.py:1077` зовёт `notify`, чья обёртка добавляет вторую строку и зовёт `mascot.say`, затем `web.py:1080` зовёт `mascot.say` снова. Вторая копия без медиа | **закрыто** build-22: консьюмер зовёт БАЗОВЫЙ нотифаер, а не обёртку. Обёртка `notify` существует для того, что приходит в обход ленты — она сама добавляет строку и говорит её питомцу; событие уже лежало в ленте со своим медиа, поэтому обёртка клала вторую копию БЕЗ него и заставляла питомца сказать всё дважды. Тест считает и строки, и произнесённые фразы |
| A-14 | P1 | **`bridge_media/` на Pi не чистит никто.** `io.py:846` копирует файл на каждый снимок с `target='computer'`, читателя два, удаления нет ни одного (grep по всему репозиторию робота). Рядом в том же файле есть потолок 1 МБ для журнала событий — шаблон существует, каталог медиа его пропустил. Плюс имя `{ts}-{basename}` перетирает файл, снятый в ту же секунду | **закрыто** 2026-09-02 в репозитории робота (`582cb99`): `_prune_bridge_media` — два порога, потому что дешёвая карта кончается двумя способами (возраст 24 ч и общий объём 64 МБ, самое старое первым; оба через env). Имя получило шесть шестнадцатеричных знаков — секундной точности не хватало, два кадра в одну секунду давали одно имя. Уборка fail-open отдельно от съёмки, проверено тестом. 2165 тестов робота зелёные |
| A-15 | P1 | **Правило «`mcp` бампается вместе с `HERMES_VERSION`» — проза без гейта, в двух копиях.** `pyproject.toml:10` и `scripts/build_app.sh:104` держат `mcp==1.26.0` независимо; тестов на парность нет, хотя парность оболочки и payload гейтится. `requirements/periphery.txt:17` робота уже требует `mcp>=1.27` | **закрыто** build-22: пара объявлена в `[tool.vibebridge.wire]`, скрипт сборки её ЧИТАЕТ (второго литерала нет), и три проверки в `tests/test_wire_parity.py`: пин сцеплен с зависимостью, в сборке нет своего пина, объявленный Hermes сверяется с `orchestrators.env` робота. Кросс-репозиторная проверка пропускается С ПРИЧИНОЙ там, где дерева робота нет, а не зеленеет молча; гейт проверен подсадкой. **Премиса находки поправлена:** `periphery.txt` (`mcp>=1.27`) — ДРУГОЙ MCP-процесс на плате (периферийный сервер), контрагентом моста он не является; настоящий контрагент — мозг, `HERMES_VERSION=0.19.0`, и пин `1.26.0` с ним в парности. Расхождения нет |
| A-16 | P1 | **Числа, зашитые в код, спорят с config.toml.** `AutoUpdater` создаётся без `interval_s`/`settings` (`app.py:57`), поэтому `update.interval_hours` не действует; `refusal_text` жёстко говорит «(60 секунд)» независимо от `ask_timeout_s` (`consent.py:155`); карточка телефона и визард используют `BRIDGE_PORT`, а не `settings.port` (`web.py:481,554`) — при смене порта команда `tailscale serve` и адрес на карте неверны | **закрыто полностью** build-22. Треть закрыта в build-21 (`refusal_text` берёт настоящий `ask_timeout_s`). Здесь: `start_autoupdate` получает настройки явно, поэтому `update.interval_hours` и выключатель действуют — совпадение умолчаний (6 ч там и там) прятало это, настройка «работала», пока её не меняли. И веб-слой больше не держит своей константы порта; заведён механический гейт против возврата класса |
| A-17 | P2 | **`screenshot` отдаёт полноэкранный base64 текстом.** Без масштабирования и потолка (`capabilities.py:94`); FastMCP сериализует dict в text content, никогда в `ImageContent` — 3–11 МБ base64 в контекст мозга на плате с 4 ГБ. Аргумента `scale`/`max_width` нет | **закрыто** build-22: снимок ужимается системным `sips` (масштаб + JPEG одним вызовом, без единой сторонней зависимости в подписанном бандле) и проходит общий потолок `encode_screenshot` — 1,5 МБ на data-URL, иначе говоримый отказ «попросите меньшую ширину». Появился аргумент `max_width`, и он КЛАМПИТСЯ: приходит от мозга, а не от владельца. Потолок общий для трёх паков, и механический гейт запрещает собственный `b64encode` в каждом. Провал `sips` деградирует в исходный PNG — снимок нужнее идеального размера |
| A-18 | P2 | **Поле `channel`, добавленное роботом ДЛЯ моста, мост выбрасывает.** `io.py:890` пишет его с комментарием «без этого лента не отличает показ на компьютере от зеркала телеграма»; консьюмер моста пересобирает dict из `ts/kind/text/media` (`web.py:1067`) | **закрыто** build-22 вместе с A-13 (одна функция — править её дважды значило бы возвращаться): `channel` больше не выбрасывается консьюмером, а лента помечает зеркало телеграма словом. Ровно то, ради чего робот это поле и кладёт |
| A-19 | P2 | **Историю событий не держит никто, и обе стороны ссылаются друг на друга.** Робот стартует хвост с EOF, докстрока: «историю панель берёт из своего журнала» (`bridge_api.py:197`); мост держит `deque(maxlen=50)` только в памяти и чистит её на новой сессии питомца (`web.py:330,804`). Всё сказанное роботом при выключенном мосте исчезает молча | **закрыто** build-23 + робот `276d626`: у моста появился `vibebridge/feed.py` — append-only JSONL рядом с журналом, потолок и truncate-ротация как у аудита, дедупликация по (ts, kind, text), загрузка при старте. Новый разговор больше не стирает сказанное: вместо стирания в ленту кладётся ГРАНИЦА «— новый разговор —». Робот при подключении досылает последние 20 строк (кламп до 200, число приходит из сети). Тест, закреплявший ложь «старые ходы остаются в журнале», переписан |
| A-20 | P2 | **Медиа втягивается в ОЗУ моста без потолка**, а все отказы называются одним 404 «файл недоступен» — офлайн-робот, неверный токен, отсутствующий файл и обход каталога неотличимы (`robot.py:167`, `web.py:467`). Видео ни одна сторона по размеру не ограничивает | **закрыто** build-23: `RobotClient.media` читает тело ПОТОКОМ с потолком 25 МБ и отказывает по объявленному `Content-Length` до первого байта; возвращает структуру вместо `tuple | None`, поэтому пять исходов различимы — `unconfigured`, `bad-name`, `unauthorized`, `not-found`, `too-large`, `unreachable`. Эндпоинт раскладывает их по статусам (400/404/413/502/409) и отдаёт причину словами. Внутренностей моста наружу по-прежнему нет |
| A-21 | P2 | **`/pair` отдаёт `mcp_url` на порту-константе**, а не на действующем (`web.py:586` против корректного `web.py:867`); та же константа течёт в `serve_active` и в команду `tailscale serve`. Не ломается только потому, что `creds.mcp_url` на роботе не читает никто | **закрыто** build-22 вместе с A-16 (одна константа, три утечки): `/pair`, карточка телефона и старт визарда берут `settings.port`. `serve_active` и команда `tailscale serve` — тоже: раньше проверка «HTTPS уже включён» смотрела на чужой порт |
| A-22 | P2 | **Пейринг-токен без TTL и без ротации.** `pending_pair_token` персистится и валиден через рестарты вечно (`state.py:57`); `state.robot_token = state.robot_token or …` (`web.py:564`) — повторный пейринг переиспользует старый токен, пути ротации нет, робот-замена наследует кредитив предшественника. Робот гасит токен до того, как сохранит результат (`bridge_pair.py:145`) — неудачная запись оставляет мост спаренным, робота нет. *После A-2 дверь ограничена тейлнетом, но TTL и ротация остаются* | **закрыто** build-23 + робот `b28ad7c`: у одноразового токена появился срок (`consent.pairing_ttl_hours`, 24 ч по умолчанию) и отдельный отказ — «истёк» и «подделан» разные новости, и в первом случае владельцу надо выдать новый, а не искать врага. Постоянный ключ теперь СВЕЖИЙ на каждый пейринг: `or` переиспользовал старый, и робот-замена наследовал кредитив предшественника. Перепейринг И ЕСТЬ путь ротации — робот получает новый ключ в том же ответе. Робот пишет пробу до предъявления токена: провал записи ПОСЛЕ гашения оставлял мост спаренным, а робота нет, и второй попытки не было |
| A-23 | P2 | **Текст карточки «Робот» неверен и пугает:** «Подключение появится с визардом; робот уже может действовать на этом компьютере» (`index.html:268`) — визард уже есть, а вторая половина читается как «неизвестный робот уже имеет руки на моём Маке» | **закрыто** build-23: обе половины строки были неверны, и вторая ещё и пугала. Новый текст говорит, чего именно не будет (статуса, чата, событий), и что вызов инструментов — отдельный канал, от пейринга не зависящий. У карточки появился ПУТЬ: кнопка открывает «Настройки» и прокручивает к форме подключения. **Брендовый проход НЕ делался** — пака `docs/brand/` в проекте нет, а его заведение это B-5; здесь исправлена ложь, а не поставлен голос |
| A-24 | P2 | **Одним кликом переключается граница безопасности без объяснения** (`index.html:432`): в gateway `/mcp` не проверяет токен вовсе, и предупреждение появляется только ПОСЛЕ перезапуска. Панель не говорит, какие интерфейсы слушает мост | **закрыто** build-23: кнопка сначала ОБЪЯСНЯЕТ, потом переключает (взвод, как у «Обновить робота»), а последствие показано ДО клика — раньше предупреждение считалось от ЖИВОГО режима, то есть от того, который владелец уже покинул. Панель называет, какие интерфейсы мост слушает (`listens`/`bind_host`) — это и есть переключаемая граница. `alert()` убран: он блокирует страницу и ничего не объясняет; причина провала уходит туда же, где стоит объяснение |
| A-25 | P2 | **`notify` — класс READ с наружным эффектом**, единственная READ-возможность, которая пишет на экран владельца; rate-limit'а нет ни на одной стороне, тормоз только пауза | **закрыто** build-23: у `notify` появился тормоз — скользящее окно `RateLimit` (6 в минуту по умолчанию, `consent.notify_per_minute`, ноль = без тормоза). Отказ говоримый и с числом секунд. Между «пусть показывает» и kill switch, то есть «выключить всё», раньше не было НИЧЕГО. Класс закрыт общим лимитером, а не правкой одной функции: это первая READ-способность с наружным эффектом и вряд ли последняя |
| A-26 | P2 | **Время в меню-баре сдвинуто на смещение зоны** — `app.py:265` режет ISO-строку, ровно та ошибка, которую в панели уже исправили `localTs`; и там одна строка вместо «последних нескольких», обещанных README | **закрыто** build-23: формат времени и строк живёт в `audit` (`local_hhmmss`, `recent_lines`) и один на обе поверхности — две реализации одной беды и были причиной, по которой фикс панели не долетел до меню-бара. Показываются ПЯТЬ строк подменю, как обещает README, а не первая. Провал перестройки меню уходит в журнал, а не в тишину. Заведён гейт: `app.py` больше не режет ISO-строку сам |
| A-27 | P2 | **Телефон: мёртвая cookie ведёт на macOS-only дверь.** Cookie сессионная, без `max_age` (`web.py:328`), `start_url` манифеста без токена — после перезапуска PWA на iPhone владелец читает «нажмите значок в меню-баре». Плюс `sw.js:47` превращает любой `!r.ok`, включая 401, в «запрос уже решён» | **закрыто** build-23: кука панели живёт 90 дней (`_set_panel_cookie`, одна точка на обе двери), а `Secure` ставится ТОЛЬКО по https — на loopback по http он выбросил бы её молча. Дверь перестала быть macOS-only: у неё появился абзац для телефона с настоящим путём восстановления. `sw.js` отличает 401 от «уже решено» — раньше владелец шёл искать несуществующий ответ вместо починки входа. `start_url` намеренно оставлен без токена: это был бы второй секрет во втором месте, а чинит проблему срок жизни куки |
| A-28 | P2 | **Настройка телефона требует терминала** — `tailscale serve --bg` копипастой из панели (`index.html:508`), против визии §4 «он не настраивает туннели и не читает конфиги» | **закрыто** build-23: `net.serve_enable` + `POST /api/phone/serve` + кнопка «Включить HTTPS для телефона». Команда осталась видимой мелким шрифтом — она честно говорит, ЧТО именно мост сделает. Слова `tailscale` уходят владельцу как есть, кроме одного случая, который он объясняет плохо: Serve выключен в админке тейлнета, и починить это можно ТОЛЬКО там. Попутно снята копия поиска бинаря — `serve_active` искала его сама вместо `_tailscale_exe()` |
| A-29 | P3 | **Совет в сообщении «порт занят» не работает:** предлагает поменять порт в config.toml (`vbboot/runner.py:127`), а гвард читает `VIBE_BRIDGE_PORT` или константу и config.toml не читает никогда (`vbboot/__main__.py:68`) | open |
| A-30 | P3 | **Робот не спарится, если Мак спал 5 минут, и повторить нельзя:** 30 попыток по 10 с, затем `provision.done` и `disable` выполняются безусловно (`wizard.py:128`), а панель после записи карты больше ничего не говорит | **половина закрыта** build-20: `provision.done` и `disable` только после удачного пейринга, окно 60×10 с, повторный запуск не пережигает рабочий токен (`--status`). Экран ожидания в панели — это U-7 |
| A-31 | P3 | **20 инструментов там, где 10 возможностей**, и skill робота называет только устаревшую половину `mac_*` (`robot_skills/mac_bridge.md:16`) — B-7 нельзя закрыть внутри одного репозитория. Цена сегодня: удвоенные токены определений в каждом запросе к мозгу | open |

| A-32 | P1 | **Регрессия доступности прошла бы зелёной.** `test_the_pet_breathes_but_stops_when_asked` проверяет `"prefers-reduced-motion" in page`, а строка есть в файле дважды — в правиле и в ПОЯСНИТЕЛЬНОМ КОММЕНТАРИИ. Доказано подсадкой: удаление настоящего `@media`-правила оставило оба ассерта зелёными | open |
| A-33 | P1 | **Тест моторной доктрины смотрит только ПЕРВЫЙ шаг каждого `@keyframes`** — блок режется по первой `}`. Доказано подсадкой: `width`/`height` в шаге `50%` прошли банлист. Плюс доктрина сканирует только `mascot.js`, тогда как анимации есть и в `mascot.html` | open |
| A-34 | P1 | **`@keyframes vb-breathe` определён дважды с разными значениями:** `mascot.js:129` (−2.5px, токенная кривая) против `mascot.html:42` (−3px, зашитый `ease-in-out`). Две копии одной правды — тот самый класс, который проект уже переучивал на палитре и шкале | open |
| A-35 | P1 | **Мой предохранитель от настоящих уведомлений закрывает один канал из двух.** `conftest.py` перехватывает `subprocess.run` — ветку osascript; основной macOS-бэкенд `desktop_notifier` идёт через UNUserNotificationCenter без subprocess. Тест, дошедший до основной ветки, поставит владельцу настоящий тост, и предохранитель промолчит — ровно тот провал, ради которого он написан | open |
| A-36 | P1 | **У самого частого класса дефектов проекта нет механической проверки.** Ретро называет проглоченную ошибку «третьим случаем»; в отгружаемом коде остаётся 10 молчащих обработчиков (`web.py:324`, `capabilities.py:149`, `audit.py:36,59,70`, `autostart.py:55`, `vbboot/layout.py:89,100,169`, `vbboot/__main__.py:61`). Каждый прошлый случай закрыт СВОИМ тестом; следующий не предотвращён ничем | open |
| A-37 | P2 | **У класса «написано, протестировано, не вызвано» тоже нет проверки** — три случая (`prune`, `migrate_from_state`, `top_up`), и единственные гейты это grep по точному тексту исходника, которые разоружает любое переформатирование | open |
| A-38 | P2 | **`esc()` — экранировщик ТЕКСТА, применяемый в позиции АТРИБУТА:** он не экранирует `"`. Используется внутри кавычек (`index.html:506`, `mascot.js:177`), а `aria-label` в `mascot.js:92,110` подставляется вообще без экранирования. Сегодня не эксплуатируется, но `test_phone.py:191` закрепляет уязвимый шаблон как правильный | open |
| A-39 | P2 | **Ни одна строка JS не исполняется набором** — ~90 ассертов это grep по тексту. `node` доступен и уже применялся вручную. `localTs()`, чей баг «UTC как настенные часы» отгрузился, прикрыта только `assert "function localTs(" in html` | open |
| A-40 | P2 | **Ссылки `file:line` в леджерах устают за один коммит.** `verification.md:26` ссылается на `index.html:599`, строка уже `:639`; `verification.md:21` называет `mascot_window.py`, которого нет; `conftest.py:26` называет тест по старому имени. Собственное правило проекта (доказательная документация) не сделано механическим | open |
| A-41 | P2 | **`capabilities._notifier` — процессный глобал, который `build_app` пишет и никто не восстанавливает**; десятки тестов зовут `build_app`, и часть затем дёргает `_notifier` напрямую, полагаясь на то, чьё приложение победило | open |
| A-42 | P3 | **Нет запрета тестам шеллиться в CLI `tailscale`** — класс, уже дважды стоивший набору времени (10 с → 4 мин). Сегодня остаётся 2 настоящих вызова, от большего спасает только TTL 60 с | open |
| A-43 | P3 | **Тесты, переспецифицированные на константы и точные строки исходника** (`width:300px`, `VB_DUR = 220`, целые JS-выражения с пробелами): кодируют текущее значение вместо свойства из докстринга — ломаются от переформатирования, не ломаются от смены поведения | open |

### Угол архитектуры (F-NN)

| ID | Приоритет | Находка и доказательство | Статус |
|---|---|---|---|
| F-1 | P0 | **Гвард второго экземпляра не работал на дефолтной конфигурации — ИЗМЕРЕНО.** Проба шла `bind(("127.0.0.1", port))` с `SO_REUSEADDR`, а payload с 2026-09-01 биндит `0.0.0.0` (standalone — дефолт): на BSD точечный бинд поверх wildcard с `SO_REUSEADDR` разрешён, и гвард отвечал «порт свободен». Второй экземпляр стартовал МОЛЧА — два трея, два питомца, консент только в одном процессе, в журнале ни строки. Плюс оболочка охраняла `VIBE_BRIDGE_PORT` или константу и `config.toml` не читала никогда | **закрыто** build-19: проба на wildcard и без `SO_REUSEADDR`, порт из `config.toml` через `tomllib`; 3 теста, измерены оба вида бинда |
| F-4 | P1 | **`/api/version` объявлял откат обновлением — ИЗМЕРЕНО.** `pending = installed if installed != running` — сравнение СТРОК: после установки нового DMG каталог payload остаётся на прошлой версии, и панель обещала «обновление скачано и применится после перезапуска», хотя перезапуск ничего не менял | **закрыто** build-19: чистая `pending_version()` со сравнением версий, 5 случаев в тесте |
| F-7 | P2 | **Репозиторий релизов — две правды, панель показывала не ту:** константа `update.RELEASE_REPO` против настройки `release.repo`; `/api/version` отдавал константу, `/api/settings` — настройку. Тест закреплял неверный источник | **закрыто** build-19: `/api/version` берёт настройку, тест проверяет форк-репозиторий |
| F-2 | P1 | **`build_app` — замыкание на 895 строк с 44 вложенными функциями**, ни одна не тестируется отдельно; внутри переопределение собственного параметра `notify`, читающее переменные, объявленные ниже. Это корень пяти уже открытых находок (A-11, A-13, A-16, A-21, A-41): каждая правится точечно, потому что проверить логику иначе как через полный HTTP-стек нельзя | open |
| F-3 | P1 | **Авторизация панели — 34 копии одной проверки на 42 маршрута**, и нет теста, который перебирает `app.routes` и требует 401 без куки для всего непубличного. Новый маршрут уедет публичным, и ни один тест не заметит | open |
| F-5 | P1 | **Оболочка знает, какой код выбрала, и выбрасывает ответ.** `Chosen(path, version, source, fell_back)` строится и не передаётся в `run()`; payload переугадывает источник по наличию каталога и врёт при равных версиях. `fell_back` живёт в одном тосте «подробности в журнале» — при том что vbboot по построению не может писать в журнал, и записи там нет | open |
| F-6 | P1 | **Фикс A-1 лёг только в macOS-копию.** Контракт инструментов объявлен ТРИЖДЫ (`capabilities._build_darwin`, `platforms/linux.py`, `platforms/windows.py`). `POWERSHELL_BLOCKED` не содержит синтеза нажатий (`SendKeys`, `System.Windows.Forms`), а `automation` там не показывает скрипт — то есть на первой же Windows-сборке инструмент делает то, что продукт объявляет невозможным | open |
| F-9 | P1 | **Шов оболочка↔payload не объявлен.** `SHELL_MIN = "0.1.0"` заморожен на первом релизе, тогда как `vbboot` менялся после него трижды (в одном из них `layout.resolve` был переименован); payload импортирует шесть символов оболочки, а `vbboot.__all__` перечисляет только `layout`. Payload на старой оболочке упадёт `AttributeError`, а не понятным «нужен новый .app» | open |
| F-8 | P2 | **Функция про устройство подписанного бандла живёт в веб-слое:** `web._bundle_resources` — приватный символ, который импортируют `app.py` и через него вся ветка автообновления; тот же обход `parents` заново написан в `tray.py`. Якорь доверия обновлений достаётся через приватную деталь модуля HTTP-маршрутов | open |
| F-10 | P2 | **`BridgeState.load` перечисляет 14 полей руками**, тогда как `save()` берёт `asdict` — новое поле пишется на диск и молча теряется при каждом рестарте | open |
| F-12 | P2 | **У `ConsentEngine` нет наблюдателя, поэтому «что нового» определяют три независимых поллера** по-разному (диф снапшота против множества id). Докстрока обещает замену поллинга хуками — не заменено | open |
| F-11 | P3 | **HTTP-обработчик вызывает другой обработчик и разбирает обратно его тело** (`api_wizard_prepare` → `api_wizard_pairing_start` → `json.loads(bytes(...))`): побочный эффект (выпуск одноразового токена) спрятан внутри «получить значение» | open |
| F-13 | P3 | **Мёртвый код и бесхозные дефолты:** `update.RELEASE_FEED`/`DOWNLOAD_BASE`, `RobotUnconfigured` (не поднят ни разу), `desktop.DEFAULT_SIZE`, `robot._last_online` (пишется, не читается), `mascot.STATES` (вторая правда рядом с настоящей цепочкой), и **`autostart.open_settings` — написана, покрыта тестом, не вызвана ниоткуда: ЧЕТВЁРТЫЙ случай класса A-37**. Плюс порт `48620` дважды внутри `config.py` | open |

### Угол визуала (V-NN)

| ID | Приоритет | Находка и доказательство | Статус |
|---|---|---|---|
| V-1 | P1 | **Палитра живёт в четырёх копиях, а тест видит две поверхности из четырёх.** `offline.html` держит хексы руками и `h1{font-size:20px}` — размера нет во всей шкале; `_DOOR_HTML` в `web.py` — свою палитру и свои размеры; `manifest.webmanifest` — третью копию фона. «Дверь» — первое, что видит человек без токена; offline — то, что он видит, когда всё сломалось; обе не смотрел никто | open |
| V-2 | P1 | **Пауза окрашена в амбер, зарезервированный за «нужен человек».** Тумблер паузы красится `--warn`, и в сорока пикселях рядом тем же `--warn` красится точка на ОЖИДАЮЩИЙ запрос — один цвет, два значения. `mascot.js` красит паузу нейтралью: две поверхности говорят о паузе разное | open |
| V-3 | P1 | **Таймер-бар согласия задан паком до пикселя и не построен:** 4px, заливка `--warn`, дренаж слева направо за 60 с линейно, при reduced-motion — цифра секунд. Второй signature-элемент пака. Смыкается с A-9, но строить нечего, кроме уже спроектированного | **закрыто** build-21 вместе с A-9 (борд сам отмечал, что это одна работа): бар 4px, `--warn`, `width:var(--left)` от данных, дренаж линейный; тест смотрит на ПРАВИЛА с вырезанными комментариями |
| V-4 | P1 | **У панели нет ни одной раскладки для телефона.** Единственный `@media` — про широкие карточки; пак требует до 900px одну колонку и вкладки снизу. Шапка `display:flex` без `flex-wrap` выпирает и тянет боковой скролл, а PWA — заявленный канал с манифестом, service worker и пушами | open |
| V-5 | P1 | **В плавающем виджете нет фокус-ринга вообще** — ни одного focus-правила, при том что это поверхность, на которой принимается решение по согласию. Пак: «2px `--accent`, offset 2px — на всём» | open |
| V-6 | P1 | **Питомец в своём окне не произносит ни одного слова о состоянии:** виджет рисует фигуру напрямую, минуя `renderMascot()`, и теряет даже `title`. Поправка пака: «он не заменяет собой текст: рядом с ним всегда слово о состоянии». На паузе и офлайне движения нет по доктрине — и слова нет по этому дефекту | open |
| V-7 | P2 | **Тумблер паузы анимирует `left`, то есть раскладку** — тот класс, который уже чинили в маскоте, остался на signature-элементе панели. Живой пример того, что пропускает A-33 | open |
| V-8 | P2 | **Формы визарда и привязки не одеты вовсе:** правил для `.field`, `label`, `input` нет ни одного. Единственный экран, где владелец ВВОДИТ адрес робота, ключ и пароль Wi-Fi, выглядит необработанной HTML-формой | open |
| V-9 | P2 | **Красный баннер настроек в тёмной теме не проходит AA: 4.36:1** (посчитано). Единственная красная строка, сообщающая, что граница безопасности стоит неверно. Пак сам называет эту полосу и решает её правилом — правило применили к акценту, не применили к опасности | open |
| V-10 | P2 | **Тёмная тема существует только как медиа-запрос:** заявленный паком `data-theme` и оверрайд light/dark/system не построены; единственное вхождение `data-theme` в проекте — в превью пака, то есть превью умеет то, чего не умеет продукт. Поэтому тёмную тему нельзя проверить не меняя настройки ОС — поэтому V-9 и дожил до аудита | open |
| V-11 | P2 | **Primary и destructive кнопки не отвечают на наведение** при объявленном `transition` — две из трёх кнопок решения молчат под курсором | open |
| V-12 | P2 | **Длинная строка события ломает журнал горизонтально:** `.row` без `min-width:0` у детей; соседний `.kv .v` это правило имеет — знали и применили в одном месте из двух | open |
| V-13 | P2 | **Длинный ответ робота ограничен в виджете и не ограничен в панели**, и в панели показывается сырая разметка: виджет прогоняет ответ через `fmt()`, панельный пузырь берёт `esc()`. Плюс речь робота идёт размером `--t-cap`, документированным как «подписи карточек» | open |
| V-14 | P3 | **Радиусы, тени и размер точки вбиты руками** там, где есть токены и число из пака (точка 7px в виджете против 8px в панели и 8px в паке); три разных `rgba(0,0,0,…)` без токена, и в тёмной теме чёрная тень под тёмной панелью не даёт ничего | open |
| V-15 | P3 | **`aria-selected` стоит на кнопках, которые не табы** (фильтры журнала, переключатель путей привязки) — для скринридера выбранный фильтр не выбран; сами табы без `aria-controls`, виды без `role="tabpanel"`, стрелки в tablist не работают | open |

### Механические проверки, которые предложил аудит визуала

Все семь сформулированы как СВОЙСТВА, а не как значения — чтобы не добавлять новых случаев A-43: список поверхностей включает `offline.html` и `_DOOR_HTML`, фон манифеста сверяется с токеном (V-1); литералов радиусов и теней в CSS нет (V-14); фокус-ринг объявлен в `tokens.css`, файле, который достаёт до всех поверхностей (V-5); раскладочные свойства не анимируются на ВСЕХ поверхностях, `@keyframes` режется по шагам, банлист распространён и на `transition:` (V-7 и A-33 одним тестом); у каждого класса из разметки есть правило — единственная проверка, которая поймала бы неодетые формы (V-8); амбер допускается только в белом списке селекторов (V-2); контраст считается арифметикой WCAG по реально используемым парам в обеих темах (V-9).

### Угол сценариев и JTBD (U-NN)

**Факт, объясняющий половину находок:** `docs/ux/scenarios.md` не менялся с
2026-08-31, а после него в репозиторий пришло **восемь коммитов, меняющих
user-facing поведение**, и ни один не тронул канон. `vision.md`,
`foundation.md`, `flows.md`, `screens.md` не менялись с 2026-08-29. Шапка
сценариев требует обратного: «Update in the same change as any user-facing
behavior change».

| ID | Приоритет | Находка и доказательство | Статус |
|---|---|---|---|
| U-1 | P0 | **Пауза с панели не доходила до меню-бара, а клик по пункту «поставить на паузу» её СНИМАЛ.** Тексты меню писал только обработчик клика; `_poll` обновлял лишь эмодзи. Пауза, поставленная с панели или телефона, оставляла пункт «⏸ Поставить на паузу», и клик исполнял `paused = not True`. Kill switch инвертировался на единственной отгруженной ОС; в pystray-ветке для Win/Linux то же сделано правильно — лямбдой. Заодно строка была неверна по существу: пауза выключает ЭТОТ компьютер, робот продолжает жить | **закрыто** build-19: тексты — производное от состояния, единственный писатель `_sync_pause_labels()` в `_poll`; строка «Поставить мост на паузу» |
| U-2 | P1 | **Мост держит контекст разговора, а оба интерфейса говорят обратное.** `chat_history` с `deque(maxlen=20)` отправляется мозгу; SCN-008 шаг 3 дословно: «bridge контекст не ведёт — мозг остаётся у робота»; визия §5.5 то же. И панель, и виджет ПИШУТ владельцу «контекст разговора живёт у мозга робота, не на панели». Решить один раз: переписать сценарий и обе строки под «короткий живой контекст сессии» — или убрать историю. Недопустимо оставшееся: интерфейс сообщает противоположное коду | open |
| U-3 | P1 | **Отказы двух новых границ не оставляли строки в журнале** — при том что визия §3 обещает журналировать «исполненное И отклонённое», а метрика JTBD-04 та же | **закрыто** build-19: `_RefusalJournal` — одна строка на различный отказ, повтор не чаще раза в минуту (иначе сканер вытеснит из журнала всё остальное); 3 теста |
| U-4 | P1 | **Карточка «Система робота» — это форма, которую FLW-03 отверг словами:** «дашборд с графиками метрик (CPU, память, температура) — отвергнут: это мониторинг для инженера, не пульт для владельца». Отгружено ровно это, B-26/B-30 закрыты как done, канон не тронут. Либо записать разворот решения и его причину, либо срезать карточку до трёх вопросов пульта | open |
| U-5 | P1 | **Две кнопки в «Доступ и настройки» не дают обратной связи:** `pending` сравнивает 6 полей из 10 — `ask_for_read`, `mascot_window`, `mascot_skin`, `robot_repo` в списке нет. Клик пишет файл, строка показывает старое значение, ноты «ждёт перезапуска» нет. Нарушены SCN-025 шаг 2 и SCN-023. Тест закрывает только `mode`, поэтому живая проверка прошла через рестарт и промолчала | open |
| U-6 | P1 | **Канон противоречит сам себе про чат офлайн**, и код реализует одну половину: SCN-007 требует отключить ввод, SCN-009 — отправить с пометкой «не доставлено»; `screens.md` держит оба состояния. Отгружен SCN-009, а кнопка «Обновить робота» офлайн не рисуется вовсе — то есть не «отключена с причиной», а отсутствует | open |
| U-7 | P1 | **Экран SCR-07 «Пейринг» — семь состояний, ни одной точки реализации.** Нет экрана ожидания, нет «Робот найден», нет чеклиста из четырёх пунктов, нет диагностики по таймауту. Четвёртый пункт чеклиста — «согласие работает (тестовый ACT)» — единственная проверка основного обещания продукта, и её нет. B-9 покрывает степпер визарда, не этот экран | open |
| U-8 | P2 | **«Не мессенджер» (визия §6) обойдён через сценарий, минуя визию.** Отгружены серверная лента, сессии с кнопкой «Новый» и превью ВЛОЖЕНИЙ — image/video/audio. SCN-026 это легализовал; визия не менялась, а `mascot.py` до сих пор несёт в коде противоположную доктрину. Назвать границу словами («лента — сессия, а не архив») либо срезать ленту | open |
| U-9 | P2 | **SCN-026 шаг 4 описывает одно-оконный виджет, от которого отказались:** «окно подрастает под виджет и снова ужимается» — ровно та конструкция, чей провал вызвал переделку в два окна | open |
| U-10 | P2 | **Пауза глушит эффект, но не присутствие:** поллер статуса стучится к роботу каждые 10 с и SSE-консьюмер держит соединение независимо от паузы — пауза проверяется только перед `notify`. Принцип 3: «kill switch делает устройство неотличимым от выключенного». С точки зрения робота выключенный компьютер звонит ему каждые десять секунд | open |
| U-11 | P2 | **Две поверхности чата не видят друг друга, а «Новый» в виджете стирает контекст панели:** ответ из панели не попадает в ленту виджета, разговор виджета не попадает во вкладку «Чат», а `POST /api/mascot/session` чистит `robot_events` и `chat_history` ЦЕЛИКОМ, включая сессию панели | open |
| U-12 | P2 | **SCN-010 шаг 3 не реализован, шаг 2 не реализован нигде:** тост приходит всегда, даже когда панель открыта; клик по уведомлению на десктопе не делает ничего (`on_clicked` не задан), в PWA фокусирует окно без анкера на ленту | open |
| U-13 | P2 | **«Разрешить на 15 мин» — зашитая копия над настраиваемым грантом:** `grant_ttl_s` это настройка, но все четыре поверхности говорят «15 мин». Меняешь настройку — кнопка обещает не то, что произойдёт | open |
| U-14 | P2 | **SCR-08 «Настройки»: две из пяти групп отсутствуют** — нет строки про уведомления и состояние системного разрешения (канал известен, но пишется в журнал, а не в панель), от подписчиков показан только счётчик без списка и без отзыва; error-ветка SCN-010 не реализована | open |
| U-15 | P2 | **Интерфейс зашивает имя робота одного владельца:** «Вася» в шести местах кода и как дефолт обеих форм. `status.name` существует и в компаньоне используется правильно — данные есть, лента и карточка их игнорируют. Для другого владельца продукт зовёт его робота чужим именем | open |
| U-16 | P2 | **Согласие принимает тот, кто ближе к экрану, а канон обещает обратное.** `foundation.md`: «согласие спрашивается у владельца, а не у того, кто ближе к экрану» — а окно питомца висит поверх разблокированного Мака с тремя кнопками. И ни один из 27 сценариев не относится к персоне P-03 (член семьи) | open |
| U-17 | P2 | **Поверхности теряют состояния, которые канон перечисляет:** SCR-05 различает «решено с другой поверхности» и «истёк», код сливает их в одну фразу; SCN-011 обещает в строке журнала класс READ/ACT и краткий результат — запись их несёт, разметка не рисует, при том что фильтры ACT/READ рядом | open |
| U-18 | P3 | **`screens.md` устарел целиком:** все девять экранов «Coverage: none yet» при отгруженных трее, панели, журнале, карточке согласия, настройках, визарде и PWA; все девять ссылаются на `wireframes/SCR-0N.md` — каталога нет; три пути к токенам и компонентам не существуют. Собственный линтер проекта уже кричит 5×`U057` — и это никем не читается | open |

### Сценарии, которые надо описать (из угла UX)

| ID | Суть |
|---|---|
| SCN-028 | Робот стучится до пейринга или не из тейлнета: что получает (401 `unpaired` / 403) и что видит владелец в журнале |
| SCN-029 | «Система робота» на пульте: какие цифры показываем и почему это не мониторинг — или отказ от карточки (U-4) |
| SCN-030 | Виджет двумя окнами: что где живёт, голова не двигается ни от чего, кроме драга |
| SCN-031 | Лента виджета: одна лента на ответы, события и уведомления; граница «сессия, а не архив»; что делает «Новый» |
| SCN-032 | Медиа от робота: превью, прокси через мост, честные отказы (нет файла / обход каталога / робот офлайн) |
| SCN-033 | Питомец помнит своё место: позиция переживает перезапуск и переезд между экранами |
| SCN-034 | Канал, из которого пришло слово: «сказано на компьютере» против зеркала телеграма (нужен под A-18 и B-32) |
| SCN-035 | Мост слушает не только loopback: что владелец должен знать про standalone и что показывает панель (нужен при любом исходе B-37) |
| SCN-036 | Канал уведомлений и его отказ: чем показано, что делать, когда система запретила |
| SCN-037 | Настройка изменилась и ждёт перезапуска: единое поведение всех кнопок панели (канон под U-5) |
| SCN-038 | Член семьи у экрана (P-03): что видит, что нельзя, какое допущение принято про разблокированный Мак |

**Плюс три экрана, которых в `screens.md` нет вовсе:** SCR-10 «Питомец (голова)»,
SCR-11 «Компаньон — лента виджета», SCR-12 «Окно приложения».

**Правки существующих:** SCN-007 шаг 2 (U-6), SCN-008 шаг 3 (U-2), SCN-010 шаг 3
(U-12), SCN-026 шаг 4 (U-9), FLW-03 rejected shape (U-4), и FLW-01 — второй
отвергнутый кандидат («всё делает официальный Imager, мы даём инструкцию»)
отгружен именно в отвергнутом виде.

## Порядок работ

**Волна 1 — доделать критическое (P0).** A-5 (визард не доводит пейринг),
A-6 (двойные побочные эффекты), A-7 (`KeyError` и перетёртый контекст). Это
три места, где продукт ведёт себя неверно, а не просто неудобно. F-1 из этой
волны закрыт в build-19.

**Волна 2 — честность согласия (P1).** A-8, A-9, A-10, A-11: владелец должен
понимать, ЧТО разрешает, СКОЛЬКО у него времени и почему ничего не
произошло. Это ядро визии, и именно оно сейчас размыто.

**Волна 3 — честность привязки и лент (P1).** A-12, A-13, A-14, A-15, A-16.

**Волна 4 — пределы и границы (P2).** A-17…A-25, плюс F-8, F-10, F-12.

**Волна 4б — визуальный слой.** Сначала то, что меняет ПОНИМАНИЕ: V-2 (амбер
паузы), V-6 (питомец без слова о состоянии), V-5 (фокус-ринг), V-3
(таймер-бар — он же A-9). Потом V-4 (телефон), V-1 (четыре копии палитры),
V-8 (неодетые формы), V-9/V-10 (контраст и выбор темы), затем V-7, V-11…V-15.

**Волна 4в — швы и контракты.** F-2 (разобрать `build_app` — он корень пяти
находок), F-3 (авторизация роутером, а не 34 копиями), F-5 (оболочка
докладывает выбор), F-6 (одна таблица инструментов на три платформы), F-9
(объявить API оболочки и разморозить `SHELL_MIN`).

**Волна 5 — телефон, тексты, мелочи (P2–P3).** A-26…A-31 + старые B-5, B-8,
B-9, B-12, B-16, B-22.

**Волна 5б — канон в правду.** U-2 и U-4 требуют РЕШЕНИЯ, а не правки:
интерфейс сегодня утверждает противоположное коду, а отгруженная карточка —
это форма, которую флоу отверг словами. Дальше: одиннадцать новых сценариев,
три новых экрана и шесть правок существующих; `screens.md` привести в правду
или снять — линтер уже пять раз сообщает, что покрытие не измеряется.

**Отдельно, не работа над кодом:** B-14 (удалить лишний приватный
репозиторий) и B-15 (отозвать app-specific пароль) — требуют владельца;
B-37 — решение по биндингу; и **повторить аудит по пяти непокрытым углам**,
когда лимит расходов позволит запустить субагентов.
